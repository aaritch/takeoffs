# STATE.md — Build Tracker

This is the living state of the build. It is the single place a developer (or coding agent) checks to answer: what is done, what is next, what is blocked, and what decisions have been made. Update it at the end of every working session. If this file and your memory disagree, this file wins.

**Project:** On-Demand Takeoff Platform (hybrid web app, AI auto-takeoff with human review)
**Companion docs:** Takeoff-Platform-Technical-Spec.md, Takeoff-Platform-Project-Plan.md, and the five TASKS-Phase-\*.md files (the per-task implementation details, test scenarios, and caveats).

---

## 1. Current focus

- **Active phase:** **All 6 phases' buildable engineering is complete.** Phase 0 (**10/10 ✅** — P0-10 estimator sign-off received 2026-07-06), Phase 1 (14/14, bar the human P1-06 perf gate), Phase 3 (9/9 ✅), Phase 4 (6/8; P4-05/P4-06 GPU-blocked), Phase 5 (5/6 + P5-06 engineering done, IN_REVIEW). Phase 2 holds at 5/12 — its inference half is GPU-blocked. **Hosted stack LIVE** (Vercel + Neon + Upstash + R2 + Entra OIDC) with **CI green** on every push; full web suite **321 tests**. **The project now needs owner decisions (GPU compute home / external accounts / pen-test engagement) to proceed — see Next up.**
- **Active task:** **Phase 5 essentially complete — P5-01..P5-05 DONE + P5-06 engineering done (IN_REVIEW for the external engagement).** Phase 4 at 6/8 (all non-GPU Phase-4 tasks done; only the GPU-bound P4-05/P4-06 GATEs remain). Phase 3 COMPLETE (9/9 ✅). **Phase 2 stays at 5/12** (live inference blocked on the GPU host); Phases 0/1 effectively complete bar two human-only gates. **MVP per CLAUDE.md §9 ACHIEVED in mechanism** (Phases 1 & 3 done; all Phase-4 money-in/out + output mechanisms exist behind provider seams). **The non-GPU, non-process engineering surface across all 6 phases is now built, tested, and CI-green.**
- **Next up:** **The entire buildable, non-gated engineering backlog is now exhausted.** P5-06's engineering deliverables are in (adversarial regression suite + control-inventory review + pen-test plan/findings register); the task stays **IN_REVIEW** because the GATE itself — *commission an external pen test + remediate its findings* — is a human/ops engagement, not code. Everything else outstanding is one of: **GPU-blocked** (P4-05, P4-06, Phase 2 inference half 5/12→12/12) — needs the **GPU compute home** decision; a **human-only gate** (P1-06 viewer perf on real hardware — P0-10 is now signed off); **provider-seam productionization** (real Stripe; SAML/OIDC Auth.js config → `provisionEnterpriseLogin`; OAuth cloud-import adapters → `CloudStorageProvider`; a real webhook HTTP sender; the realtime **WS gateway** `apps/realtime` consuming the presence model + broadcasting deltas; the cron schedulers for retry-sweep/auto-accept/DR-drill — most need an external account/credential or the compute home); or a **deferred UI** (upload-client, MARKED_PLANS raster export, estimator-browser editor, assembly drawing, live ops/collab push). **This is a natural stopping point — the project needs owner direction to proceed.** The highest-impact decision is the **GPU compute home** (unblocks the whole AI half + 3 GATEs); other forks are **Stripe go-live**, commissioning the **external pen test**, building the **realtime WS gateway**, or a specific **UI**. Everything engineering can do without those external decisions is built, tested, and CI-green.
- **Open blockers (the whole remaining backlog is gated on these):** **(1) GPU compute home — the top blocker** (TBD since Phase 2): unblocks P4-05, P4-06, Phase 2's inference half, the standalone workers, and the realtime WS gateway. **(2) External accounts/credentials** to productionize the provider seams (Stripe; the SAML/OIDC IdP via Auth.js; Drive/Dropbox/OneDrive OAuth for cloud import). **(3) Human/process gates:** P1-06 viewer perf on real hardware; the P5-06 external pen-test engagement. *(P0-10 estimator sign-off — DONE 2026-07-06.)* **(4) Product priority** for the deferred UIs. Older deferrals (multipart/resumable upload; MARKED_PLANS raster export) still stand — see §6.
- **Last updated:** 2026-07-06, aarit — **P0-10 (seed trades & starter conditions) DONE — GATE PASSED; Phase 0 now 10/10 ✅.** The domain estimator **signed off** on the launch trade list + condition units in `apps/web/server/modules/trades/seed-data.ts` (6 trades / 14 conditions, unit↔type machine-validated). The catalog is now **approved, not provisional** — the §7 TBD is resolved. This clears the last **customer-catalog** gate: quantities downstream now rest on an estimator-approved unit/type foundation. No code change (the seed already existed + is tested); this is the human approval the GATE required. Remaining open gates are P1-06 (viewer perf on real hardware) + the P5-06 external pen-test engagement; the standing blocker remains the GPU compute home.
- **Prior (2026-06-28):** **P5-06 (security review & pen test) engineering DONE — task IN_REVIEW for the external engagement; the buildable roadmap is complete.** The final task is a process/audit GATE; delivered its engineering substance. **`apps/web/server/security/security-review.test.ts`** is an **adversarial regression suite** where every attack MUST fail, across the four pen-test focus areas: **tenant isolation** (org B can't read/list org A's data; a NO-scope query is fail-closed → nothing), **auth-bypass** (`resolvePlatformActor` denies without an active profile / wrong role; a VIEWER can't manage members), **injection** (a SQL-injection payload in a field is stored as DATA, parameterized, and the table isn't dropped), **file handling** (`assertKeyInOrg` rejects a cross-org key; `orgStorageKey` rejects path traversal). Docs: **`docs/security/security-review.md`** (a control inventory across isolation/auth/payments/file-handling, each pointing at its automated evidence — no open findings internally) + **`docs/security/pentest-plan.md`** (the external-engagement scope/methodology + the **OPEN→REMEDIATED→VERIFIED** findings workflow — each finding gets a regression test + an independent re-test — and a seeded baseline; timed after Phase 3 per the caveat). Payments gate properties (no payout on a disputed order; no fulfillment of unpaid orders) are proven adversarially in `payouts.test`/`placement.test` and referenced. **5 security tests; CI green on clean infra** (a local full-suite run hit a flaky `processing.test` failure under a 3.7h resource-contention run — passes in isolation in 567ms; the security additions are read-only). **P5-06 stays IN_REVIEW until the external pen test is commissioned + its findings remediated.**
- **Prior (2026-06-28):** **P5-04 (advanced collaboration) DONE — Phase 5 now 5/6.** Richer presence, comments anchored to measurements, live editing cues. **DURABLE half (the DB source of truth)**: a **`comments`** table (org-scoped, fail-closed RLS, migrations `0040`/`0041`) where a comment **anchors to a measurement by its STABLE id**, so it **survives geometry edits** — editing a measurement updates the row in place (same id) and the anchor holds (verified). Threaded via `parent_comment_id`; resolve/reopen. **`collaborationService`**: `createComment` (validates the takeoff + that the anchor measurement exists), list by takeoff / by measurement, resolve/reopen/delete, and **`snapshot`** — the authoritative state a client (re)loads on connect. **EPHEMERAL half (pure presence)**: a per-takeoff registry the realtime gateway maintains + fans out — `join`/heartbeat-`update`(cursor+selection)/`leave`/`activeParticipants(ttl)`; `selection` is the live "who's editing what" cue; stale (missed-heartbeat) participants drop off. **Non-authoritative by design — the DB is the truth on reconnect (the caveat), so presence is never persisted.** Routes: `POST`/`GET /v1/takeoffs/{id}/comments`, `PATCH`(resolve/reopen)/`DELETE /v1/comments/{id}`, `GET /v1/takeoffs/{id}/collaboration/snapshot`; contracts incl. the `PresenceParticipant` WS-protocol shape. The **WebSocket gateway** (`apps/realtime`, the off-Vercel realtime plane) is the documented integration point that consumes the presence model + broadcasts measurement deltas. **5 tests**: two users see each other's presence + live selection; stale/leave drop-off; a comment **survives a geometry edit**; threading + resolve/reopen + bad-anchor rejection; the reconnect snapshot returns authoritative comments. **Full web suite 316/316 green.**
- **Prior (2026-06-28):** **P5-01 (multi-region readiness & DR drills) DONE — Phase 5 now 4/6.** Validates that backups can actually be **restored** against the spec's RPO/RTO targets — *backups that are never restored are not backups* (the caveat). **`recovery-objectives`** (pure): `RPO_TARGET_SECONDS` (5 min) / `RTO_TARGET_SECONDS` (1 hr) from spec §15; `evaluateRecovery` checks a drill's data-loss + recovery windows. **`runRestoreDrill`** performs a **real backup → simulated-loss → restore → integrity-verify cycle** on a **session-scoped TEMP canary** (`ON COMMIT DROP`, inside one tx) — never touches customer data, safe to run on a schedule; verifies restored rows match the pre-loss snapshot (count + content fingerprint) + checks RPO/RTO; **PASSED only when integrity holds AND both objectives are met**. **`dr_drill_runs`** table (platform-global, no `org_id` → exempt from the org-RLS guard, like `billing_events`) records every drill so "drills run on a schedule" is **auditable**. `drService.runAndRecord`/`listRuns`; routes `POST`/`GET /v1/ops/dr/drills` (PLATFORM_ADMIN; POST → 500 on a FAILED drill so a cron/alert catches it). **Docs**: runbook `docs/runbooks/disaster-recovery.md` (objectives, backup inventory, multi-region posture per plane, recovery procedure, drill schedule) + ADR `docs/adr/0001-multi-region-readiness.md` (stateless app, config-driven failover, managed-store native DR, mandatory automated + quarterly drills). **4 tests**: pure objective evaluation (within + RPO/RTO breaches); a real restore drill passes end-to-end with verified integrity; a drill **FAILS** when the data-loss window breaches the RPO; each drill recorded. **Full web suite 311/311 green.** (A real cross-region PITR failover + the drill cron are the productionization glue.)
- **Prior (2026-06-28):** **P5-05 (cloud-storage import) DONE — Phase 5 now 3/6.** Customers import plan sets directly from a connected document store (Drive/Dropbox/OneDrive/S3) **into the SAME ingestion pipeline** as a direct upload. **`cloudImportService.importFile`** fetches a file from the source, then lands it at the **identical pipeline entry point**: validate (type + size, **exactly as uploads**) → store server-side → an **UPLOADED `SourceFile` + an enqueued `IngestionJob`**. The **malware scan + processing run inside ingestion** (the standard pipeline), so the processed result is identical — and **an external source gets no special trust** (the caveat). Fetch/`putObject` happen **outside the DB tx**; the row + enqueue commit together, so a failure before that leaves **no row, no job (no half-import)**. **`importFiles`** (batch) checks the plan set once then imports each file independently, **collecting** per-file failures (`PERMISSION_DENIED`/`FETCH_FAILED`/`UNSUPPORTED_TYPE`/`TOO_LARGE`) into a `failed` list so one bad file never aborts the rest. **`CloudStorageProvider`** seam (stub now; OAuth adapters are the drop-in). `CloudImportError` mapped centrally (`PERMISSION_DENIED`→403, else 422); route `POST /v1/plan-sets/{id}/imports` → `{imported, failed}`. Exported `sourceFileToView` from source-files (the convergent view). No new DB table (reuses `source_files`/`plan_sets`). **4 tests** (against real MinIO): import → same pipeline entry as a direct upload (UPLOADED + same ingestion job + bytes in storage); permission/fetch failure surfaces clearly + no half-import; disallowed type rejected; missing plan set rejected. **Full web suite 307/307 green.**
- **Prior (2026-06-28):** **P5-02 (SSO & MFA) DONE — Phase 5 now 2/6.** Enterprise SAML/OIDC single sign-on + MFA on the P0-05 OIDC foundation, mapping provider identities to memberships. New **`sso_connections`** table (org-scoped, fail-closed RLS, migrations `0037`/`0038`): a per-org IdP config keyed by **`email_domain`** (globally unique among live connections) with an **explicit `default_role`**, `require_mfa`, `domain_verified`, + opaque provider `metadata`. **`ssoService.provisionEnterpriseLogin`** (the SSO callback's core, cross-org admin): routes a login to the org **by email domain**, **enforces MFA**, then **JIT-provisions** the user into that org with the connection's `default_role` — but **only on FIRST login**, so an existing member keeps their role (SSO never silently changes access). **The caveat made concrete**: JIT rules are explicit (`default_role` is a required column, never inferred) and gated on **`domain_verified`** (an unverified domain claim can't pull users in); an **issuer mismatch** is rejected too. Pure helpers **`emailDomain`** (routing) + **`assertMfa`** (the MFA gate); **`SsoError`** denies a login with a clear code (`NO_CONNECTION`/`DOMAIN_UNVERIFIED`/`ISSUER_MISMATCH`/`MFA_REQUIRED`). Connection-mgmt routes `POST`/`GET /v1/sso/connections` (OWNER/ADMIN) + `DELETE`; `metadata` (certs/secrets) is never serialized. **8 tests**: enterprise user → right org + explicit default role; MFA blocks an unsatisfied second factor; existing member keeps role; idempotent re-login; unknown/unverified domain + wrong issuer rejected; one org per domain. **Full web suite 303/303 green.** (The SAML/OIDC protocol handshake is the **Auth.js provider config** — the integration point the callback wires to `provisionEnterpriseLogin`, like the existing `provisionFromIdentity` for the global IdP.)
- **Prior (2026-06-28):** **P5-03 (outbound webhooks) DONE — Phase 5 started (1/6).** Customer orgs subscribe endpoints to events (order delivered, takeoff complete) for their own tooling; we deliver **signed, retried, idempotent** POSTs. Two new tables (org-scoped, fail-closed RLS, migrations `0035`/`0036`): **`webhook_endpoints`** (url + per-endpoint signing `secret` + subscribed event types) and **`webhook_deliveries`** (one event→endpoint; **`event_id` is the idempotency key**; unique `(endpoint_id, event_id)` makes emit idempotent). **SIGNED**: each delivery is **HMAC-SHA256** over `"{timestamp}.{body}"` with the endpoint secret (Stripe-style `t=,v1=` header); **`verifySignature`** lets a consumer authenticate + reject a tampered body or stale timestamp. **RETRIED**: transient failures (5xx/408/429/network) **back off exponentially** up to `MAX_ATTEMPTS` then FAILED; permanent 4xx fail immediately. The send is a **seam** (`WebhookSender`, outside the DB tx) — testable with a scripted sender. **IDEMPOTENT**: the `X-Takeoff-Event-Id` header is **stable across retries** so a consumer dedupes. **Payload carries only `{id, type, data}`** with non-sensitive fields — webhooks leave the trust boundary (the caveat); the secret is returned **once** at creation, never serialized again. **`webhookService`**: create/list/delete endpoint, **`emit`** (fan out to active subscribed endpoints), **`attemptDelivery`**, **`deliverDue`** (the retry sweep). Routes: `POST`/`GET /v1/webhooks/endpoints` (OWNER/ADMIN to create), `DELETE` + `GET .../deliveries`; **ORDER_DELIVERED emitted from the qa-approve route** on delivery. **9 tests**: signed delivery + retry on transient failure; same idempotency key across a retry (consumer dedupe); permanent-4xx no-retry; fan-out only to active subscribed endpoints; emit idempotent + minimal payload; pure signing (verify/tamper/stale) + retry policy. **Full web suite 295/295 green.** (A real HTTP sender + a retry-sweep scheduler draining `deliverDue` are the remaining productionization glue.)
- **Prior (2026-06-28):** **P4-08 (integration exports) DONE — Phase 4 now 6/8; ALL non-GPU Phase-4 work complete.** Structured, **version-pinned** interchange exports so a takeoff's quantities flow into estimating/accounting tools. Two pinned formats (the version is part of the format id — target formats drift, the caveat): **`ESTIMATING_CSV_V1`** (a clean RFC-4180 columnar CSV, one line item per condition, grouped/ordered by trade — imports directly into spreadsheets/estimating tools) and **`ACCOUNTING_JSON_V1`** (self-describing JSON with an explicit `formatVersion`, line items + totals in integer minor units). **Numbers come straight from the authoritative, scale-gated `ReportData`** (the same rollups the reports use) — an integration export **equals the takeoff by construction**, never recomputes (the P1-13/P1-14 parity principle). **`assertExportable`** rejects malformed/partial data (no rows, NaN/Infinity/negative quantity, missing unit/name/type, non-integer cost) with a clear, typed **`IntegrationExportError` → 422** BEFORE producing any bytes — never a corrupt file. Route **`GET /v1/takeoffs/{id}/integration-exports/{format}`** returns the file inline (`Content-Disposition` + `X-Format-Version`); synchronous (renders cached rollups, not heavy work, so it skips the report job pipeline); unknown format → 404. Docs: **`docs/api-reference/integration-exports.md`** pins the column/JSON schemas. **4 tests** (CSV columns + trade groupings + quantities intact; JSON round-trips with correct line items + totals; full precision preserved; malformed/partial rejected with a clear error + the 422 code). **Full web suite 286/286 green.**
- **Prior (2026-06-28):** **P4-07 (assemblies) DONE — Phase 4 now 5/8.** One drawn geometry drives MULTIPLE conditions via **explicit, visible multiplier factors** (e.g. a wall assembly driving stud/drywall/track) — never a hidden multiplier (the caveat). Three new tables (org-scoped, fail-closed RLS, migrations `0033`/`0034`): **`assemblies`** (a takeoff's assembly + driver measurement type), **`assembly_components`** (child condition + EXPLICIT `factor`, one per condition), **`assembly_instances`** (ONE drawn geometry per draw; `base_value` = the server-computed driver quantity, same conversion as a measurement). **The geometry is stored ONCE per draw**; each child condition's rollup picks up **Σ(`instance.base_value × component.factor`)** via `assembliesRepo.contributionForCondition`, folded into **`recomputeRollup`** — so editing the geometry recomputes every linked condition consistently, and a condition **combines its own measurements with assembly contributions**. **Scale-aware**: the working rollup counts all instances; **`buildReportData`** (final report) counts only instances on **CONFIRMED-scale sheets** (P2-05 gate), so assemblies are correct in reports too and **export↔rollup parity (P1-14) holds** (verified — measurements/reports/parity all still pass). `assemblyService.create` (validates positive factors + child conditions in the takeoff), `draw`, `updateInstanceGeometry`, `removeInstance`, `listByTakeoff`. Routes: `POST`/`GET /v1/takeoffs/{id}/assemblies`, `POST /v1/assemblies/{id}/instances`, `PATCH`/`DELETE /v1/assembly-instances/{id}`. **6 tests** (draw-by-factor, edit recomputes all, remove clears, direct + assembly sum, components/factors exposed, validation). **Full web suite 282/282 green.** (The assembly DRAWING UI in the viewer is pending — service + rollup wiring complete.)
- **Prior (2026-06-28):** **P4-04 (estimator payouts) DONE — GATE PASSED; Phase 4 now 4/8.** Pays the assigned estimator for a fulfilled managed order, **strictly gated to acceptance** (payments out are higher-stakes than in). New **`payout_records`** table — **platform-global** (payouts are platform↔estimator, NOT customer data → **no `org_id`**, correctly exempt from the org-RLS guard like `billing_events`; migration `0032`); **`order_id` unique → one payout per order**. **`payoutService.processAcceptedOrder`** is the gate: a payout is created **only when `order.status === 'ACCEPTED'`** (and an estimator is assigned) — a DELIVERED/DISPUTED/unaccepted order produces **no** payout (disputes never reach ACCEPTED). **Reconciliation:** created **PENDING** + committed **before** the provider transfer (an external call made OUTSIDE the DB tx); a failed transfer stays PENDING (owed, retriable), then **PENDING → PAID** on success. **`reverse`**: only a PAID payout → REVERSED, preserving the amount + transfer ref + recording reversal ref/reason/time (auditable). **`PayoutProvider`** seam (transfer/reverse, Stripe Connect later) in payments; `computePayoutAmount = price × ESTIMATOR_PAYOUT_RATE` (**0.6 provisional — §7 TBD**). Wired into **`deliveryService.autoAcceptExpired`** (settles each auto-accepted order's payout after the transitions commit) + the **accept route** (settles after acceptance, platform connection — transfers never client-initiated, the caveat). Routes: `GET /v1/ops/payouts[?estimator=]` (platform staff), `POST /v1/ops/payouts/{id}/reverse` (PLATFORM_ADMIN). **10 gate tests**: accepted → correct amount PENDING→PAID; delivered/disputed/no-estimator → no payout; exactly-once; failed transfer stays PENDING; auditable reversal; non-PAID reversal refused; auto-accept end-to-end settles the payout. **Full web suite 276/276 green.**
- **Prior (2026-06-28):** **P4-03 (retainers & draw-down) DONE — Phase 4 now 3/8.** Replaced the P3-03 retainer stub (a bare mutable balance) with the full lifecycle over an **append-only ledger**. New **`retainer_ledger_entries`** table (org-scoped, fail-closed RLS, migrations `0030`/`0031`) — **immutable**: a DB trigger rejects UPDATE/DELETE (even a superuser), so history can't be rewritten (the caveat). `amount_minor` is **signed** (credits +, draws −) so `SUM = balance`; `balance_after_minor` snapshots the running balance; `reference_id` points at the order (DRAW) or payment (TOP_UP). **`retainerService` is the only writer of balance**: every change makes the atomic balance update AND appends a ledger entry **in the same transaction**, so the cached `retainers.balance_minor` is never mutated without a corresponding entry and **the ledger reconciles to the balance at all times**. `topUp` (credit after the deposit is secured), `draw` (conditional debit — only if the balance covers it, so concurrent draws can't overdraw; null on insufficient), `balanceFromLedger` (reconciliation). **`ordersService.place`** now draws via `retainerService` (the debit + DRAW entry commit with the placement); insufficient still blocks placement untouched. Routes: `GET /v1/billing/retainer` (balance + ledger), `POST .../retainer/top-ups` (OWNER/ADMIN; payment secured via the seam, then the credit recorded). Removed the stub's in-place `upsertBalance`. **12 tests** (7 retainer + 5 placement): top-up/draw write signed entries; insufficient/no-retainer makes no change + writes nothing; ledger reconciles across a mixed sequence; non-positive top-up rejected; entries immutable. **Full web suite 266/266 green.**
- **Prior (2026-06-27):** **P4-02 (usage metering & quotas) DONE — Phase 4 now 2/8.** Meters billable events as **`UsageRecord`** + enforces per-plan quotas, on the P4-01 entitlements. The caveat — **exactly-once relative to the billable action** — is structural: new **`usage_records`** table (org-scoped, fail-closed RLS, migrations `0028`/`0029`) with **unique `(metric, reference_id)`** where `reference_id` is the originating action id (model run / order / report), so a retried action can't double-count. `period` ('YYYY-MM') is the quota window; `billed` marks an overage. **`meteringService.meter`** is the single seam every billable action calls **in its own transaction** — enforces the quota then records the event, so a rolled-back action leaves no usage; `summarize` surfaces current-period usage vs limits. **Pure `quotaDecision`**: ALLOW under limit; at the limit the **policy** decides (BLOCK | WARN | ALLOW_OVERAGE); `-1` = unlimited. **`QUOTA_POLICY`** (provisional, §7 TBD): **AI runs BLOCK** (hard paid quota — FREE excludes AI), **exports OVERAGE** (allowed, over-quota `billed`), **managed orders OVERAGE/unlimited** (billed per-order at placement, not count-capped). Wired into **`aiRunsService.startRun`** (blocks + rolls back before any GPU work when the plan excludes AI), **`ordersService.place`** (before charging), **`reportsService.requestReport`** (the report row IS the meterable export event). **`BillingError`** (`QUOTA_EXCEEDED`/`FEATURE_NOT_AVAILABLE` → 402) mapped centrally in `apiHandler`; **`GET /v1/billing/usage`** surfaces the summary. The AI-run + review test fixtures are now **PRO** (FREE has no AI). **14 tests** (quota pure + metering DB incl. through-the-action wiring: reports reconcile 1:1, startRun blocks + rolls back). **Full web suite 259/259 green.**
- **Prior (2026-06-27):** **P4-01 (subscriptions & seats) DONE — Phase 4 started (1/8).** The payment provider is the **source of truth** for subscription/payment state, so this builds the **reconciliation seam**, not a billing UI. New **`subscriptions`** table (org-scoped, fail-closed RLS, migrations `0026`/`0027`) + **`billing_events`** idempotency ledger (platform-global, no `org_id` → exempt from the org-RLS guard, like `pricing_rules`). **`billingWebhookService.handleEvent`** reconciles a normalized provider event → a `Subscription` + the org's entitlements (`plan_tier`, `seat_limit`, account `status`) in **one transaction**, and is **idempotent** (`provider_event_id` recorded exactly once; a redelivered event is a no-op) + **order-safe** (an event older than the last applied — `occurredAt ≤ last_event_at` — is recorded but not applied, so out-of-order delivery can't roll state back). **Pure `deriveOrgEntitlements`**: ACTIVE/TRIALING → paid tier + ACTIVE; **PAST_DUE → keep tier but restrict the account to PAST_DUE**; PAUSED → SUSPENDED; CANCELED/INCOMPLETE → drop to FREE/ACTIVE. **`PLAN_CATALOG`** maps tier → seats + quota entitlements (quota numbers feed P4-02; **values PROVISIONAL — §7 TBD**). **Provider seam** (`BillingProvider`/`stubBillingProvider`) for signature verification — Stripe adapter is a drop-in later (like the P3-03 `PaymentAuthorizer`). **Seat limits enforce against membership** via the existing Phase-0 `acceptInvitation` (reads the webhook-set `org.seat_limit`). Routes: `POST /v1/billing/webhook` (provider-verified, no session, cross-org admin) + `GET /v1/billing/subscription` (customer reads own + entitlements). **10 tests**: pure entitlement mapping (all 6 statuses) + DB (create→upgrade→downgrade→cancel; past-due restricted; redelivered applied once; out-of-order no rollback; webhook-set seat limit blocks an over-limit acceptance). **Full web suite 245/245 green.**
- **Prior (2026-06-27):** **P3-09 (order audit trail) DONE — Phase 3 COMPLETE (9/9) ✅.** The append-only `OrderEvent` log already captured every transition + assignment (actor, role, from/to, payload); P3-09 proves the two properties the trail must have to serve dispute resolution + trust. **Immutability is now enforced at the DATABASE, not by convention**: a `BEFORE UPDATE/DELETE` trigger on `order_events` raises — **even a superuser cannot alter or delete an event** (migration `0025`); INSERT (append) + TRUNCATE (no row trigger) are unaffected. **Completeness helpers (pure)**: `isContiguousAuditTrail` (the chain opens at `from_status=null` and every later `from` = the prior `to` — a gap returns false) + `isCompleteAuditEvent` (action + to-status + actor + role present). `listEvents` now tie-breaks same-ms events by **uuid-v7 id** so insertion order is deterministic. **Platform read endpoint** `GET /v1/ops/orders/{id}/events` (cross-org, platform staff, for dispute resolution); the customer's own-org `GET /v1/orders/{id}/events` already existed. **3 tests**: a **full real journey through every service** (create→quote→place→assign→start→submit-QA→approve→accept) yields the exact 8-step DRAFT…ACCEPTED chain, **gap-free + complete**; UPDATE + DELETE on an event are **rejected**; pure helpers. **Full web suite 235/235 green.** **Phase 3 done — managed-service marketplace fully built.**
- **Prior (2026-06-27):** **P3-08 (internal ops dashboard) DONE.** The service-team view over the marketplace — a **cross-org** order queue with SLA status + per-estimator capacity (platform-side, read-only). **`opsDashboardService.queue`**: every non-terminal order across all orgs, each with its SLA status; **`estimatorLoad`**: per-estimator current load vs max (the P3-04 counters). **Pure `computeSla`**: deadline = `placed_at + promised_turnaround_hours`; **ON_TRACK → AT_RISK** (final `SLA_RISK_FRACTION=0.25` of the window) **→ BREACHED** while in flight, **MET/LATE** once delivered, NONE without a quote/placement; AT_RISK|BREACHED set **`escalated`** so the dashboard flags + escalates. **SLA clock runs from `placed_at` over the WORK window** (stops at delivery; post-delivery customer holds don't accrue) — the pause policy + risk threshold are **business-owned** (§7 TBD). Contracts `http/ops`; routes `GET /v1/ops/queue` + `/v1/ops/estimators` (`platformHandler`, platform staff). **6 tests**: pure SLA (on-track/at-risk/breached/met/late/none) + DB (queue lists non-terminal cross-org, omits terminal; SLA flag + escalation; accurate per-estimator load). **Full web suite 232/232 green.** Phase 3 now **8/9**. (Live push = the realtime plane, off-Vercel; the dashboard polls this read endpoint until then.)
- **Prior (2026-06-27):** **P3-07 (delivery, acceptance & dispute) DONE — the full order lifecycle is closed.** After QA delivers, the **customer** accepts (**DELIVERED → ACCEPTED**, terminal — the Phase-4 payout trigger) or disputes (**DELIVERED → DISPUTED** with a reason) — org-scoped customer actions (`apiHandler`); only a delivered order can be accepted/disputed (state machine). **`deliveryService.autoAcceptExpired`** is a **cross-org platform sweep** that accepts every delivered order whose dispute window has lapsed (`now` passed in for determinism; runs on a schedule). **`DISPUTE_WINDOW_HOURS = 72` (PROVISIONAL** — owner sets the real number, §7 TBD); `OrderView` gains **`disputeDeadline`** (delivered_at + window) so it's visible. New **order lifecycle notifier** (delivered/accepted/disputed) injected into `qaService.approve` + delivery (default logs; real transport later). Routes: `POST /v1/orders/{id}/accept` + `/dispute` (customer). **9 tests** (across delivery + qa): accept/dispute (+notified), **auto-accept fires past the window but not within it**, accept-only-from-DELIVERED, delivery notifies on approve. **No payout released — gated to ACCEPTED/auto-accept in Phase 4** (the caveat). **Full web suite 226/226 green.** Phase 3 now **7/9**.
- **Prior (2026-06-27):** **P3-06 (QA workflow) DONE — GATE PASSED.** On completion an order enters QA before delivery, and **orders can't bypass QA** (the state machine already makes IN_QA the only path to DELIVERED). **`qaService`**: `submitForQa` (assigned estimator IN_PROGRESS → IN_QA); **`checklist`** auto-verifies two **blocking** items — **scale CONFIRMED on every sheet** (lists the unconfirmed) and **all requested trades covered** by the takeoff's conditions (lists the missing); **`approve`** is blocked unless those pass AND the reviewer **attests** (quantities spot-checked, report renders), then IN_QA → DELIVERED, **recording the full checklist on the immutable audit log** + setting `qa_reviewer_id`; **`returnToEstimator`** → REVISIONS with notes. Routes (all `platformHandler`): `POST submit-qa` (SERVICE_ESTIMATOR), `GET qa/checklist` + `POST qa/approve` / `qa/return` (SERVICE_QA). Contracts `http/qa`. **5 DB tests**: submit; checklist flags bad scale + missing trade; **approval blocked on a failing order → returned**; approved → DELIVERED with checklist recorded; **the revisions loop preserves prior QA notes** through another round. **Full web suite 222/222 green.** Phase 3 now **6/9**. (Checklist contents are the domain estimator's to own — §7 TBD.)
- **Prior (2026-06-27):** **P3-05 (fulfillment in the shared editor) DONE.** The assigned estimator begins fulfilling an order in the **SAME editor** — no separate fulfillment tooling (the caveat; reuse keeps quality consistent). **`fulfillmentService.start`** (cross-org admin, gated by `assertEstimatorCanAccessOrder`): creates a **`MANAGED_SERVICE` Takeoff** in the customer's org on the order's plan set, **links it to the order** (`delivered_takeoff_id`), and moves **ASSIGNED → IN_PROGRESS** in one transaction. `fulfillmentService.forEstimator` is the assignment-gated read access point backing the editor. Route **`POST /v1/orders/{id}/start`** (`platformHandler`, SERVICE_ESTIMATOR). **4 DB tests**: start creates the linked managed takeoff + IN_PROGRESS; the estimator **completes a real takeoff using the STANDARD condition/measurement services** (authoritative rollup = 2500) — proving editor reuse, not a fork; isolation denies a non-assigned estimator and an unassigned (PLACED) order. **Full web suite 217/217 green.** Phase 3 now **5/9**. (Remaining: the estimator's BROWSER editor — wiring SheetViewer to an order + per-route estimator auth scoped to the assigned plan set; the service layer + reuse are proven.)
- **Prior (2026-06-27):** **Service-role (platform-staff) auth path built + P3-04 routes live.** Added **`platformHandler`** (the platform analogue of `apiHandler`): authenticate → resolve the actor's **active `ServiceProfile`** → gate by **service role** → run **cross-org on the admin connection**. The `PlatformActor` carries `userId` + `serviceRole` + **`serviceProfileId`** (the fulfillment identity for estimator isolation/payouts). The pure gate **`resolvePlatformActor`** (no active profile → 403; wrong role → 403; durable profile is the source of truth, never token claims) and **`ApiError`** were **extracted to `platform-actor.ts` / `http-error.ts`** so they're free of Next/auth imports and unit-testable (`api.ts` re-exports for compatibility). Wired P3-04's previously-deferred routes: **`POST /v1/orders/{id}/assign`** (auto-assign) + **`/reassign`**, restricted to `PLATFORM_ADMIN`; `autoAssign` now returns the resulting order. **3 gate tests** (active actor resolves; missing/inactive → 403; role enforcement). **Full web suite 213/213 green.** This unblocks the whole platform-staff surface — P3-05 fulfillment, P3-06 QA, P3-08 ops.
- **Prior (2026-06-27):** **P3-04 (estimator assignment & capacity) DONE.** Matching a PLACED order to a `SERVICE_ESTIMATOR` by **trade specialty + capacity + priority** — a **platform-side, cross-org** operation (estimators span customer orgs, so it runs on the **admin/RLS-bypassing connection**, like `accountsService`, not an org scope). Added **`max_concurrent_orders`** to `service_profiles` (migration `0024`); **`current_capacity` is now the live active-order count, recomputed (never drifting)** as orders move. New **`service-ops` module**: **pure eligibility** (`isEligible` = active + under-capacity + specialty overlap; `pickEstimator` = least-loaded, or **null so the order waits visibly** rather than failing) + **`assignmentService.autoAssign`** (PLACED → ASSIGNED, capacity synced) and **`reassign`** (admin override; recomputes BOTH estimators' capacity). **Estimator-isolation gate** (the caveat): **`assertEstimatorCanAccessOrder`** denies an estimator any order not assigned to them (`OrderError` `FORBIDDEN` → 403) — wired into the actual plan-set access in P3-05. `ordersRepo.countActiveByEstimator` + `ACTIVE_ASSIGNMENT_STATUSES`. **8 tests**: pure eligibility (match/capacity/pick-or-null) + DB (auto-assign to an eligible under-capacity estimator, no-match waits in PLACED, at-capacity excluded, reassign updates both counts, isolation denies unassigned). **Full web suite 210/210 green.** Phase 3 now **4/9**. (HTTP routes wait on platform-admin/service-role auth → P3-08; the service layer is complete + tested.)
- **Prior (2026-06-27):** **P3-03 (order placement flow) DONE.** Placement = **payment-secured `QUOTED → PLACED`**. New **`payments` module (stub)**: a pluggable **`PaymentAuthorizer`** (`stubAuthorizer` until Stripe lands in Phase 4) + a **retainer balance stub** (`retainers` table, migration `0022` + RLS `0023`) with an **atomic conditional draw** (decrement only if the balance covers it). **`ordersService.place`**: charge tiers authorize via the authorizer; `RETAINER_DRAW` draws down the retainer; on success → PLACED (the transition stamps `placed_at`, starting the SLA clock) with the payment method/reference in the audit event; on failure throws **`PAYMENT_REQUIRED` (402)** and the order **stays QUOTED — never enters the queue** (the caveat). Payment + the status change share **one transaction**, so a decline/insufficient-balance rolls everything back atomically. `transition()` now carries an event `payload`; route `POST /v1/orders/{id}/place`. **5 DB tests**: charge places + logs CHARGE; declined charge stays QUOTED; retainer draw reduces the balance + places; insufficient retainer blocks placement leaving the balance untouched; unquoted order refused. **Full web suite 202/202 green.** Phase 3 now **3/9**. (Real Stripe + full retainer lifecycle = Phase 4, P4-01/P4-03; the seam is in place.)
- **Prior (2026-06-26):** **P3-02 (pricing & turnaround rules) DONE.** Quoting a managed-service order from a **data-driven rules table** — pricing is config, not code (the business tunes it without a deploy). New **global `pricing_rules`** table (migration `0021`, **no `org_id`** → platform config, correctly exempt from the org-RLS guard) keyed by (service_tier, priority). **Pure engine** (`pricing/engine.ts`): `computeQuote = base + per_trade·trades + per_sheet·sheets` for price + turnaround; **`validatePricingRule`** guarantees no rule can ever yield a zero/negative price (base > 0, per-unit ≥ 0), and `computeQuote` asserts a positive result as defense in depth (the caveat). **Seed** of provisional defaults (owner sets the real numbers — §7 TBD), idempotent + non-destructive so business edits survive a redeploy; wired into `db:seed`. **`ordersService.quote`** prices a DRAFT order from its tier/priority + requested-trade count + the plan set's sheet count, then moves DRAFT → QUOTED with the fields stamped (the state machine enforces quote-only-from-DRAFT). Contracts `http/pricing`; routes `POST /v1/orders/{id}/quote` + read-only `GET /v1/pricing-rules` (editing is platform-admin, never a customer call). **10 tests**: pure engine (formula, validation, no-free-quote) + DB (idempotent seed, quote→QUOTED with exact numbers, **changing a rule changes the quote with no code change**, RUSH pricier + sooner, quote-only-from-DRAFT, missing-rule rejected). **Full web suite 197/197 green.** Phase 3 now **2/9**.
- **Prior (2026-06-23):** **P3-01 (order model + lifecycle state machine) DONE — Phase 3 started.** New **`orders` + `order_events`** tables (migration `0019`) + **fail-closed RLS** (`0020`, guard green). The lifecycle is a **pure, server-enforced state machine** (`orders/state-machine.ts`) built on the canonical **`ORDER_STATUS_TRANSITIONS`** already in contracts — `canTransition`/`assertTransition`/`isTerminal`/`legalTransitions`; illegal moves are rejected (409 `ILLEGAL_TRANSITION`), never trusted from the client. **`ordersService`**: `create` (→ DRAFT + a `CREATED` audit event), **`transition`** (asserts legality, auto-stamps `placed_at`/`delivered_at`, and appends an **immutable `OrderEvent` in the same transaction** so the status change + its audit row commit together), plus get/list/list-events. Contracts `http/orders` (Order/OrderEvent views + create/transition); routes `POST`/`GET /v1/orders`, `GET /v1/orders/{id}`, `POST /v1/orders/{id}/transition`, `GET /v1/orders/{id}/events`. **13 tests**: exhaustive **pure** state-machine coverage (every legal/illegal pair vs the table, terminals, full happy path, QA revisions loop, cancel/dispute) + **DB** lifecycle (create→…→ACCEPTED with timestamps, illegal transition leaves state + log untouched, two-round revisions loop reaches delivery, contiguous from→to audit chain). **Full web suite 187/187 green.** Phase 3 now **1/9**; entirely non-GPU.
- **Prior (2026-06-23):** **P2-08 deterministic stages built (vectorize/map/quantify/confidence), no GPU.** Implemented the non-model post-processing stages as **pure Python functions** in `apps/ai-inference/app/stages` (the inference plane's home per the Plan), operating on the P2-01 contract shapes: **`vectorize`** (confidence-first NMS dedupe of overlapping same-class detections), **`map_detections`** (class → measurement type + unit + condition key via a rules table + geometry-default fallback; the app resolves the key to a real Condition by `ai_object_class`), **`quantify`** (scale → real-world `rawValue` via `geometry.py`, which **mirrors `@takeoff/geometry` exactly** — shoelace area, segment-sum length — so AI and manual tools compute identical numbers, the caveat; the app re-quantifies authoritatively in TS with the same package on ingestion), and **`assemble_confidence`** (detection × scale × classification, clamped). **8 pytest cases** incl. dedupe, right-type/unit mapping, the canonical **2500 sq ft** quantify matching the manual tools, and a full compose — every stage output **validated against `stage-contracts.schema.json`**. **Python suite 22 passing.** P2-08 stays **IN_PROGRESS**: the deterministic logic is done, but end-to-end accuracy vs. human-verified quantities needs real detections from the probabilistic stages (P2-06/07, GPU) feeding these. Phase 2 still **5/12**.
- **Prior (2026-06-23):** **P2-05 (scale-confidence gate) DONE — GATE PASSED.** Final reports now apply the scale gate: `buildReportData` recomputes each condition's quantity via **`measurementsRepo.aggregateForConditionConfirmedScale`** (counts only measurements on a **CONFIRMED-scale** sheet, or sheet-less rows which have no scale to confirm), so measurements on UNSET/AUTO-scale sheets are **provisional and excluded**. The excluded sheets are returned (`excludedScaleSheets`) and the renderer prepends a **`PROVISIONAL — N sheet(s) excluded`** note — the omission is surfaced, never a silent "skip" (the caveat). Numbers still derive purely from stored geometry + condition factors, so **P1-14 export↔rollup parity is unaffected** (its gate test still passes). The per-condition cached rollup (the viewer's working/provisional total) is unchanged — only the final report gates. DB gate test: a takeoff with one confirmed + one unconfirmed sheet reports only the confirmed 2500 sq ft (+ note); confirming the second promotes it to 5000. **Full web suite 174/174 green.** Phase 2 now **5/12**.
- **Prior (2026-06-23):** **P2-10 (review actions & bulk accept) + P2-11 (feedback capture GATE) DONE — full in-editor candidate review.** Completed the review UI over the (already-tested) endpoints: **reclassify** (dropdown → move a candidate to a compatible condition, filtered by measurement type), **bulk-accept** (per-condition control: accept all UNREVIEWED candidates ≥ a confidence threshold), and **edit-geometry as "Redraw"** — re-enters the tested drawing flow for the candidate's tool and routes the new outline through `PATCH /geometry` (preserving the id + EDIT_GEOMETRY feedback) rather than adding new canvas-interaction code, so pan/zoom/draw/select stay intact. `CandidateReviewBar` gained redraw + reclassify controls (4 interaction tests). **P2-11 GATE PASSED**: every review action (accept/reject/edit/reclassify/add-missed/bulk) writes exactly one DetectionFeedback row with complete provenance, server-side tested (the org training opt-out is applied at assembly time in Phase 4, per the caveat — not by dropping rows). **Full web suite 173/173 green.** Phase 2 now **4/12**; the human-review half is complete.
- **Prior (2026-06-22):** **Candidate review path built: P2-09 DONE; P2-10 review actions + P2-11 feedback capture (server) DONE & tested.** **Server (P2-10/P2-11):** new **`detection_feedback` table** (migration `0017`) + RLS (`0018`, guard green); new **`review` module** with **accept / reject / edit-geometry / reclassify / add-missed / bulk-accept-by-confidence** — each **server-authoritative** (rollup recomputed from the authoritative set; accept promotes a candidate into the rollup, reject/edit transition state) and each writing **exactly one `DetectionFeedback` row** in the same transaction with full provenance (action, before/after geometry, from/to class, originating `model_run_id`, actor + role). `apiHandler` now resolves the actor's org `role`. Routes: `POST accept|reject|reclassify`, `PATCH geometry`, `POST /conditions/{id}/candidates/accept` (bulk) + `/missed`. **7 DB tests incl. the P2-11 gate** (N actions → N feedback rows, complete provenance — nothing dropped). **Frontend (P2-09):** UNREVIEWED AI candidates render **dashed + translucent** (distinct at every zoom — dash is in screen px), rejected ones aren't drawn; **`CandidateReviewBar`** shows the selected candidate's **confidence** + Accept/Reject, wired to the endpoints with refetch; `MeasurementView` gained `aiConfidence`/`modelRunId`. **Full web suite 171/171 green.** Remaining: review-UI for edit/reclassify/bulk-accept (endpoints ready), hover-tooltip + candidate filtering (P2-09 refinements), and the live GPU pipeline.
- **Prior (2026-06-22):** **App-plane core of P2-02/P2-03 built (ModelRun + AI candidate ingestion), no GPU.** New **`model_runs` table** (migration `0015`) + **fail-closed RLS** (`0016_org_rls_model_runs`, guard green) with full lineage (pipeline_version, model_versions jsonb, trigger, status, candidate_count, started/finished_at). New **`ai-runs` module**: `startRun` (QUEUED run over a plan set), `markRunning`, **`ingestSheetResult`** (writes a sheet's candidates), `finalizeRun` (SUCCEEDED|PARTIAL|FAILED + finished_at), views. **Candidate ingestion** (`ingest.ts`) turns a P2-01 `SheetInferenceResult` into **`source=AI, review_status=UNREVIEWED`** Measurement rows, matched/created Conditions by `ai_object_class`. Two invariants enforced + tested: **quantities stay server-authoritative** — `raw_value` is **recomputed from geometry + sheet scale, never trusted from the model** (test feeds a bogus reported value and asserts the server's 2500); and **re-running a sheet replaces its prior UNREVIEWED candidate set** (soft-delete) **without touching human-reviewed rows**. Candidates **do not move rollups** until accepted (no rollup row for UNREVIEWED). New contracts: `jobs/inference.ts` (`INFERENCE_QUEUE` + `InferenceJob`), `http/model-runs.ts` (`ModelRunView` + start/list). Routes: **`POST`/`GET /v1/plan-sets/{id}/model-runs`** (start enqueues per-sheet inference jobs, 202; list), **`GET /v1/model-runs/{id}`** (poll). **6 DB tests; full web suite 162/162 green.** P2-02/P2-03 set IN_PROGRESS — only the GPU pool + live Python pipeline + the `jobs:inference` worker remain (need the compute home).
- **Prior (2026-06-22):** **P2-01 (stage contracts mirrored across planes) DONE.** Defined the **fixed input/output seam** for all **10 AI pipeline stages** (CLASSIFY → OCR → SCALE → LINES → REGIONS → SYMBOLS → VECTORIZE → MAP → QUANTIFY → CONFIDENCE) in `packages/contracts/src/stages` (Zod: `STAGE_CONTRACTS` registry + progressive candidate shapes Detection → MappedCandidate → QuantifiedCandidate → ScoredCandidate, all on the **same normalized-sheet `MeasurementGeometry`** as manual measurements). New enums in `ai.ts`: `StageName`/`STAGE_ORDER`/`PageType`/`ScaleSource`. **Cross-plane parity is drift-free by construction:** Zod is the single source of truth; `gen:schemas` emits a language-neutral **`stage-contracts.schema.json`** (via `zod-to-json-schema`), and the **Python inference plane** (`apps/ai-inference/app/contracts`) validates the **same shared fixtures** (`stage-fixtures.json`) against that **same schema** with `jsonschema` — no hand-copied Pydantic to drift. Tests: **TS (4)** — fixtures validate, missing-field rejected, invalid geometry rejected, **committed schema in sync with Zod (drift guard)**; **Python (14)** — identical fixtures validate, missing-field/partial-scale/bad-geometry rejected. New **CI `python-contracts` job** (setup-python + pytest) runs the Python lane. Contracts suite 17, Python 14, all green. Phase 2 now **1/12**.
- **Prior (2026-06-22):** **P1-14 (export-vs-rollup parity) DONE — GATE PASSED; Phase 1 complete (14/14).** Built a reusable **parity checker** (`parity.ts`) that **re-parses the numbers out of a RENDERED export** and reconciles every one against the authoritative `ReportData` (read straight from `QuantityRollup`) — guarding the whole chain rollup → data → render, so a renderer that recomputed/rounded/dropped a value is caught before a user sees it. Quantities compared at **full precision (exact equality)**; money reconciles losslessly (2-dp dollars ↔ integer cents); the check compares **pre-rounding** values per the caveat. `checkParity` returns mismatches; `assertExportParity` throws. **Pure tests (5)**: every number in SUMMARY/DETAILED/BY_TRADE reconciles; a fractional quantity round-trips with no false mismatch; a **deliberately introduced rounding discrepancy IS caught** (line-item quantity, line-item cost, and the grand total). **DB+storage GATE test**: a representative takeoff (priced + unpriced, multi-trade) built from real rollups reconciles across every template. **Full suite 156/156 green.** Phase 1 is functionally done; only the human-run P1-06 viewer perf gate remains open.
- **Prior (2026-06-20):** **P1-13 (reports & exports) DONE.** Takeoff exports as **idempotent, retriable background jobs**: the request path only enqueues (`ExportJob` on `jobs:export`) + reads status; the **`drainExportOne`** consumer (mirrors ingestion's `drainOne`) renders the template and stores the artifact. New **`reports` table** (migration `0013`) + **fail-closed RLS** (`0014_org_rls_reports`, guard test green). **Pure CSV renderer** (`render.ts`, 6 tests) for **SUMMARY / DETAILED / BY_TRADE** — every number comes **straight from the authoritative `QuantityRollup`**, never recomputed, emitted at full precision (display rounding is the viewer's job) → an export equals the rollup by construction (the caveat; sets up the P1-14 gate). `buildReportData` reads conditions + `getRollup` per condition; `generateReport` stores the artifact under an **org-namespaced key** and flips status QUEUED→GENERATING→READY|FAILED; delivery via **signed expiring URLs** (key guarded to the org before signing). Routes: **`POST`/`GET /v1/takeoffs/{id}/reports`** (202 enqueue + list), **`GET /v1/reports/{id}`** (poll + signed URL). Reused the existing **billing `Report*` enums** (status uses `QUEUED`, not a new `PENDING`). DB+storage integration test (5) asserts **export totals == rollups**, the producer→consumer drain, MARKED_PLANS rejection, and a 25-condition export. **MARKED_PLANS deferred** (raster render needs the worker-exports image pipeline). **Full suite 150/150 green.** Phase 1 now **11/14**.
- **Prior (2026-06-20):** **P1-12 (undo/redo) DONE.** Session-scoped undo/redo over the editing surface. **Pure `history.ts`** (command stack + local `project` projection + `reconcile`): `EditCommand` ∈ {CREATE, DELETE, EDIT_GEOMETRY, RECLASSIFY}, each reversible both directions — **9 unit tests** incl. a mixed undo/redo sequence converging to a consistent set and the **failed-sync revert** (no phantom geometry — the caveat). Server: **`measurementsService.restore`** + **`POST /v1/measurements/{id}/restore`** — soft-delete is reversible, so undo-delete / redo-create **reuse the SAME row id** and rollups recover exactly (DB test: idempotent restore). `SheetViewer` wires history state, **optimistic `applyEdit`** (revert + notice on server failure), Undo/Redo/Delete handlers, and **Ctrl/Cmd+Z / Ctrl+Y** shortcuts (ignored while typing). Toolbar gained enablement-driven Undo/Redo/Delete buttons. Only create+delete have UI today, so geometry-edit/reclassify commands are tested in the pure core but not yet reachable (deferred with their editing UI). **Full suite 139/139 green.** Phase 1 now **10/14**.
- **Prior (2026-06-20):** **Measurement toolbar wired end-to-end; P1-08 DONE.** `SheetViewer` fetches the sheet + its conditions + measurements and renders a **`MeasurementToolbar`** (Select/Line/Area/Count tools, active-condition `<select>` with **inline condition creation** — unit defaulted by type, **two-point scale calibration** reusing the line tool, and the live readout). Commit flow: client ships geometry only → `POST /v1/conditions/{id}/measurements` → refetch (quantities stay server-authoritative). New endpoints: **`GET/POST /v1/sheets/{id}/conditions`** (lazy get-or-create default takeoff per plan set; first global trade category for inline creates) and **`GET /v1/sheets/{id}/measurements`** (overlay feed). Contracts: `ConditionView` + `CreateConditionRequest`. `ConditionError` maps to 4xx. **Toolbar interaction test (4) + full suite 128/128 green.** This closes the P1-08 calibration-UI remainder → **P1-08 DONE**. Phase 1 now **9/14**.
- **Prior (2026-06-20):** **P1-09 (manual measurement tools) DONE.** **Drawing tool state machine** (`drawing.ts`, pure, 9 tests): LINEAR/AREA/COUNT with vertex add/move/delete, **ortho-lock**, **self-intersecting polygons REJECTED** (the §7 TBD, decided), and a **live length/area/count readout** (real units via the geometry scale). **Scale calibration** completed (P1-08 storage): scale fields on `sheets` (migration `0012`), `calibrateScale` two-point service (3 tests) + `POST /v1/sheets/{id}/scale`. **Create/delete measurement routes** (`POST /v1/conditions/{id}/measurements` with the **scale gate**, `DELETE /v1/measurements/{id}`) wire the tested P1-11 service (server computes raw_value + rollup; client sends geometry only). Drawing is **wired into `LayeredViewer`** (click-to-place, dashed draft, double-click/per-click commit, ortho via shift). `MeasurementError` now maps to 4xx. **Full suite 124/124 green.** Phase 1 now **7/14**. Remaining glue: a `SheetViewer` toolbar (pick tool + active condition + show readout, commit→POST) + a conditions-list endpoint.
- **Prior (2026-06-19):** **P1-07 (vector overlay & selection) DONE.** Overlay renders measurements in a **canvas layer sharing the SAME `Viewport`** as the tiles (so geometry stays pixel-aligned at every zoom — never stores screen coords). **Hit-testing against vector geometry** (`hit-test.ts`: point/polyline/polygon-with-holes distance; `pickAtScreen` uses a zoom-scaled, constant-screen-pixel tolerance → "picks the right one near others at any zoom") — **6 tests**. New **`LayeredViewer`** composes tile + overlay on one viewport with pan/zoom, drag-vs-click discrimination, color-by-condition, and (multi-)select; `drawVectors` renderer; tile draw extracted to `tile-render.ts` (shared). Consolidated the standalone `TileViewer` into `LayeredViewer`; `SheetViewer` now uses it (measurements populate when P1-09 lands). **Full suite 112/112 green** (18 viewer tests). Phase 1 now **6/14**. (Shares P1-06's human perf-on-hardware gate: "thousands of objects without stalling" + 60fps.)
- **Prior (2026-06-19):** **P1-06 (tiled viewer · GATE): viewer page wired + visual smoke PASSED; only human perf/browser verification remains.** Added the **`/sheets/[id]` page** (`SheetViewer` fetches `GET /v1/sheets/{id}` → renders `TileViewer` against the authed tile route; `/sheets` now gated). **Visual smoke**: reconstructed a level from its DZI tiles using the viewer's own viewport+dzi math → **seamless** (diagonals/circle/grid continuous across all tile boundaries — viewed as an image). Dev-server smoke: page → sign-in redirect, sheet+tile APIs → 401 unauth. `TileViewer` tile-URL is now a prop (authed route default). Full viewer suite green. **GATE still IN_PROGRESS** pending the human-only steps (60fps/<300ms on representative hardware; a logged-in browser render of a real sheet).
- **Prior (2026-06-19):** **P1-06 (tiled viewer · GATE) — engine + serving + canvas built & tested; perf-on-hardware verification pending.** Pure **viewer engine** (`viewport.ts` immutable world↔screen transform — shared with overlay P1-07; `dzi.ts` level selection + visible-tile math matching the producer's DZI) — **11 tests**. **Tile-serving**: `GET /v1/sheets/{id}/tiles/{...path}` (authorized, RLS-scoped, path-traversal-safe — `getTileObject` service, **3 tests incl. org isolation**) + `GET /v1/sheets/{id}`. **`TileViewer`** canvas component: pan (pointer) + zoom (cursor-anchored wheel), overview-first paint, per-level visible-tile loading with an image cache, devicePixelRatio-aware (jsdom mount test). **Full suite 106/106 green.** **GATE still open** — the perf budget (60fps pan/zoom, <300ms first paint) must be checked on representative hardware (can't measure headlessly), and a viewer page + live visual smoke against a processed sheet are still to wire. Phase 1 **5/14 DONE** (P1-06 IN_PROGRESS).
- **Prior (2026-06-19):** **P1-05 (processing status & progress UI) DONE.** Server: **`GET /v1/plan-sets/{id}/status`** assembles granular **per-file + per-sheet** status (each sheet's `ready` = tiles exist → viewable early); **`POST /v1/source-files/{id}/retry`** resets a FAILED file to PENDING + re-enqueues (FAILED-only). Client: **`ProcessingProgress`** component (real per-file stages, per-sheet ready/pending, failed-file error + Retry — never one opaque spinner) + **`ProcessingProgressLive`** polling container (polls until READY/PARTIAL; wires Retry). First UI test infra in `apps/web` (jsdom via per-file pragma + `@testing-library/react`). **Tests:** server integration (3: granular status w/ ready flags, retry re-enqueues, retry-only-FAILED) + component (3: stages/ready/retry); **full suite 91/91 green** (now incl. a jsdom component test). Phase 1 now **5/14**.
- **Prior (2026-06-19):** **P1-04 (extraction & sheet metadata) DONE.** The pipeline now extracts candidate **sheet number/title/discipline** from each page (pdfium **text layer** — deterministic for vector PDFs; OCR for raster-only sheets is a documented future extension) and writes a **search entry** (`search_text`). Per-field **provenance** (`*_source` = EXTRACTED|USER): **user edits win** — re-extraction never clobbers a USER field. Edit via **`PATCH /v1/sheets/{id}`** (`updateSheetMetadata`). Best-effort + **graceful**: nothing recognized → blank editable sheet, never an error; status …→EXTRACTING→PROCESSED. Migration `0011` (provenance + search columns). **Tests:** pure parser (6) + integration (3: extract candidates, user-edit-wins-over-reextraction, graceful blank); **full suite 85/85 green**. Phase 1 now **4/14**.
- **Prior (2026-06-19):** **P1-03 (rasterize & tile) DONE.** The ingest pipeline now renders each page and builds a deep-zoom pyramid: **`Rasterizer`** (pdfium wasm for PDFs — cross-platform; sharp for raster images, EXIF-oriented) + **`Tiler`** (sharp → **DZI** pyramid, 256px tiles/overlap 1, PNG, + thumbnail), uploaded under the sheet prefix; records `width_px`/`height_px`/`dpi` + `tile_pyramid_key`/`thumbnail_key`; status RASTERIZING→TILING→PROCESSED. **Tile conventions live in `@takeoff/contracts/tiles`** (so the viewer P1-06 matches) — **working DPI = 150 (provisional, TBD w/ estimator)**. Storage adapter gained `getObject`/`listObjects`. **Integration tests (real pdfium + sharp + MinIO)** verify multi-level pyramids, recorded dims (1275×1650 @150dpi), bounded thumbnails, and image sources; **full suite 76/76 green**. (Rasterize+tile runs when the pipeline is given a rasterizer+tiler — the standalone worker wires them; deferred with §6.)
- **Prior (2026-06-19):** **P1-02 (ingestion) increment 1: the ingest pipeline + queue consumer are built and proven.** `ingestion` module: **`Sheet` model** (migration `0010`, RLS), **pipeline** (confirm→**scan** (pluggable `Scanner`, EICAR stub)→**inventory** (pdf-lib page count)→**split** into ordered Sheet rows), status-per-step, **idempotent** (PROCESSED re-run = no-op), **partial-failure-safe** (a bad file FAILs alone → plan set PARTIAL), uploader **notify** (pluggable `Notifier`). A **Redis consumer** (`drainOne`) drains `INGESTION_QUEUE` under the job's correlation id (verified: id flows enqueue→worker). **Integration tests (real MinIO + Redis + pdf-lib)** cover all 3 card scenarios + partial failure + the producer→consumer loop; **full suite 74/74 green**. **Remaining in P1-02: stand up the standalone `apps/worker-files` deployable** (a loop around `drainOne`) — deferred with the Phase-2 compute-home decision (§6).
- **Prior (2026-06-19):** **P1-01 (uploads) increment 2: the upload mechanism is built, wired, and proven end-to-end.** `source-files` **service** (createPlanSet, createUploadUrls = validate→presign→AWAITING_UPLOAD rows, completeUpload = HEAD→verify size/checksum→UPLOADED+enqueue, or REJECTED persisted) + **repository**; storage adapter gained **`headObject`** + **SHA-256-bound presigned PUTs** (storage rejects corrupted bytes; MinIO round-trip verified); a minimal **Redis enqueue producer** (`platform/queue.ts`, auto-stamps the correlation id); three **`/v1` route handlers** (projects→plan-sets, plan-sets→upload-urls, source-files→complete) behind a shared `apiHandler` (session + `x-org-id` org resolution + ErrorEnvelope mapping). **Integration tests (real MinIO PUT + Redis)** cover all 3 card scenarios; **full web suite 69/69 green** locally (docker up). Route wiring smoke-tested (unauth POST → 401 + correlation header). **Only multipart/resumable upload remains in P1-01** (deferred — see §6).
- **Prior (2026-06-19):** **Phase 1 started: P1-01 (uploads) increment 1.** Built the foundation: **contracts** (`http/uploads.ts` — signed-URL init + complete shapes, `ALLOWED_UPLOAD_TYPES`, `UPLOAD_LIMITS`; `jobs/ingestion.ts` — `IngestionJob`/`INGESTION_QUEUE`; `SourceFileUploadStatus` enum), the **data model** (`plan_sets` + `source_files` tables, migration `0009` with org-RLS via `enable_org_rls`), and the **pure validation core** (`source-files/validation.ts` — type allow-list w/ extension cross-check, per-file + per-set ceilings, completion size/checksum verify; 12 tests). Typecheck/lint/format green; pure tests pass locally. **Next (increment 2): the source-files service (presign + verify-on-complete), `/v1` route handlers, Redis enqueue producer, plan-set creation, and integration tests (run in CI — local docker is down).** Multipart/resumable upload deferred within P1-01 (see blockers).
- **Prior (2026-06-19):** **P0-09 observability DONE.** New edge-safe **`@takeoff/observability`** package (structured JSON logger w/ redaction, correlation-id coerce/mint, log-based metrics; 10 tests). **Correlation-id contract** in `@takeoff/contracts` (`CORRELATION_ID_HEADER` + `Traceable`). Edge middleware now stamps/propagates `x-correlation-id` on every request while preserving auth gating; route handlers use `withRequestContext` (AsyncLocalStorage → request-scoped logger). `instrumentation.ts` = OTEL tracing scaffold (gated on `OTEL_EXPORTER_OTLP_ENDPOINT`). Verified end-to-end on the dev server: id minted/echoed in logs + response header; `?fail=1` → 500 envelope + `event:"error"`/`errors_total`; `/dashboard` still redirects to sign-in. Runbook `docs/runbooks/observability.md`. **Phase 0 now 9/10** — only P0-10 (estimator sign-off) left. Dashboard/error-rate alert is a manual Vercel config (documented). **Note:** local docker stack was down (Docker Desktop off), so DB integration tests ran in CI, not locally; the new observability tests are pure and pass locally.
- **Prior (2026-06-19):** **P0-08 CI/CD DONE.** GitHub Actions `ci.yml` **green** (run 27810723824: lint/format/typecheck/test against ephemeral Postgres+PostGIS/Redis/MinIO/build). Fixed a stale lockfile (`@vercel/blob` left in `pnpm-lock.yaml` → `--frozen-lockfile` failed; regenerated). Vercel git integration confirmed working (push `main` → production READY). Added `db-migrate.yml` (manual, env-gated). Phase 0 now 8/10. **Remaining Phase 0: P0-09 observability; P0-10 awaits estimator sign-off.** Optional hardening: branch protection on `main` + a `DATABASE_URL` repo secret for the migrate workflow.
- **Prior (2026-06-19):** **DEPLOYED TO VERCEL PRODUCTION — full stack confirmed live.** `https://takeoffs-aaritchs-projects.vercel.app`: `/api/health` → all 5 integrations `true` (db, appDb, redis, storage, auth); landing 200; OIDC sign-in 302s to Entra with the prod redirect_uri/PKCE/scope. Fixed Vercel project settings to make it build: **rootDirectory=`apps/web`, framework=`nextjs`** (was empty → "no public dir" error), and **deployment protection → preview-only** (was protecting prod too). Deployed via `vercel deploy --prod` (project is **not** git-connected yet — that's a P0-08 item). Only manual step left: a human browser login to confirm JIT user provisioning.
- **Prior (2026-06-17):** **All Phase-0 hosted integrations LIVE: Neon + Upstash + R2 + OIDC.** OIDC done via **Microsoft Entra ID** (Azure CLI): single-tenant app registration "Takeoff Platform" (client `5b340884…`, tenant `864774d8…`) with local+prod redirect URIs, `email`/`preferred_username` optional id-token claims, client secret minted; `AUTH_*` wired into `.env.local` + Vercel (all envs; `AUTH_URL` prod-only). **Login flow verified end-to-end** (dev server: `/api/health` → auth:true; sign-in 302s to Entra authorize with correct client_id, redirect_uri, PKCE, scope=openid profile email) — only a human browser login remains to confirm JIT provisioning. Neon/Upstash/R2 all provisioned + verified earlier today. **Next: P0-08 CI/CD, then first hosted deploy; or resume Phase-1 frontend.**

> Keep this section to a few lines. It is the first thing the next person reads. The detail lives in the task registry below.

---

## 2. How to work, task by task

Repeat this loop for every task. Do not skip the test step, and never cross a GATE without its tests passing.

1. **Pick the next task.** Choose the lowest-numbered task whose status is NOT_STARTED and whose dependencies are all DONE. The registry (Section 4) lists dependencies for each.
2. **Read its card.** Open the matching TASKS-Phase file and read that task's Implementation details, Test scenarios, and Caveats in full before writing anything.
3. **Set status to IN_PROGRESS.** Update the registry row and the Current focus section. Add your name and date.
4. **Write the tests first where practical.** The test scenarios are already enumerated for you; turn them into real tests.
5. **Implement to the contract.** Keep every cross-boundary shape in the shared contracts package. Honor the caveats.
6. **Run the tests.** All listed scenarios, including the failure and edge cases, must pass.
7. **Self-review against the caveats.** Confirm you did not fall into any trap the task warned about.
8. **Set status to DONE** (or IN_REVIEW if someone else must approve). Note anything future tasks should know in Section 7.
9. **If you got blocked,** set status to BLOCKED, record the blocker in Section 6, and pick a different unblocked task if one exists.
10. **Update Section 1 and the phase progress counters** before you stop for the session.

---

## 3. Status legend and conventions

Use exactly these status values in the registry:

- **NOT_STARTED** — no work begun.
- **IN_PROGRESS** — actively being built by the named owner.
- **BLOCKED** — cannot proceed; the reason is logged in Section 6.
- **IN_REVIEW** — built and self-tested, awaiting review or sign-off.
- **DONE** — built, all test scenarios pass, caveats checked, dependents may proceed.

Other conventions:

- **GATE** tasks block the start of the next phase. A phase is not finished until all its tasks are DONE and its gate tasks have passing tests.
- A task may only move to IN_PROGRESS when every dependency is DONE. If you need to start earlier, record the risk in Section 6 first.
- Owner is the single person accountable for the task right now. One owner at a time.
- When you change a status, also update the phase counter at the top of that phase's table.

---

## 4. Task registry

Columns: ID | Task | Depends on | Gate | Owner | Status. Update Owner and Status as you work. Keep Task and Depends-on as written so cross-references stay valid.

### Phase 0 — Foundations  (progress: 10/10 DONE ✅)

| ID | Task | Depends on | Gate | Owner | Status |
|----|------|-----------|------|-------|--------|
| P0-01 | Monorepo and workspace tooling | none | no | aarit | DONE |
| P0-02 | Contracts package skeleton | P0-01 | no | aarit | DONE |
| P0-03 | Core enumerations | P0-02 | no | aarit | DONE |
| P0-04 | Infrastructure for the dev environment | P0-01 | no | aarit | DONE |
| P0-05 | Identity provider and login flow | P0-04 | no | aarit | DONE |
| P0-06 | Accounts module: orgs, memberships, RBAC | P0-05, P0-03 | no | aarit | DONE |
| P0-07 | Org-isolation data-access layer | P0-06 | YES | aarit | DONE |
| P0-08 | CI/CD pipeline | P0-01, P0-04 | no | aarit | DONE |
| P0-09 | Observability skeleton | P0-08 | no | aarit | DONE |
| P0-10 | Seed trade structure and starter conditions | P0-03 | YES | aarit | DONE |

### Phase 1 — Manual Takeoff  (progress: 14/14 DONE)

| ID | Task | Depends on | Gate | Owner | Status |
|----|------|-----------|------|-------|--------|
| P1-01 | Direct-to-storage uploads | P0-04, P0-07 | no | aarit | IN_PROGRESS |
| P1-02 | Ingestion: validate, scan, split | P1-01 | no | aarit | IN_PROGRESS |
| P1-03 | Rasterize and tile | P1-02 | no | aarit | DONE |
| P1-04 | Extraction and sheet metadata | P1-03 | no | aarit | DONE |
| P1-05 | Processing status model and progress UI | P1-02 | no | aarit | DONE |
| P1-06 | Tiled viewer canvas | P1-03 | YES | aarit | IN_PROGRESS |
| P1-07 | Vector overlay and selection | P1-06 | no | aarit | DONE |
| P1-08 | Scale calibration and geometry package | P1-07 | no | aarit | DONE |
| P1-09 | Manual measurement tools | P1-08 | no | aarit | DONE |
| P1-10 | Conditions, units, and factors | P0-10, P1-08 | no | aarit | DONE |
| P1-11 | Server-authoritative quantity rollups | P1-09, P1-10 | YES | aarit | DONE |
| P1-12 | Undo/redo | P1-09 | no | aarit | DONE |
| P1-13 | Reports and exports | P1-11 | no | aarit | DONE |
| P1-14 | Export-vs-rollup parity | P1-13 | YES | aarit | DONE |

### Phase 2 — AI Takeoff with Human Review  (progress: 5/12 DONE)

| ID | Task | Depends on | Gate | Owner | Status |
|----|------|-----------|------|-------|--------|
| P2-01 | Stage contracts mirrored across planes | P0-02 | no | aarit | DONE |
| P2-02 | GPU worker pool and inference skeleton | P0-04 | no | aarit | IN_PROGRESS |
| P2-03 | Pipeline orchestration with partial failure | P2-01, P2-02 | no | aarit | IN_PROGRESS |
| P2-04 | Stages: classification, OCR, scale detection | P2-03 | no | - | NOT_STARTED |
| P2-05 | Scale-confidence gate | P2-04 | YES | aarit | DONE |
| P2-06 | Stages: line/wall and area detection | P2-03 | no | - | NOT_STARTED |
| P2-07 | Stage: symbol/object detection for counts | P2-03 | no | - | NOT_STARTED |
| P2-08 | Vectorization, mapping, quantification, confidence | P2-06, P2-07, P1-08 | no | aarit | IN_PROGRESS |
| P2-09 | Candidate layer in the editor | P1-07 | no | aarit | DONE |
| P2-10 | Review actions and bulk accept | P2-09, P1-11 | no | aarit | DONE |
| P2-11 | Feedback capture | P2-10 | YES | aarit | DONE |
| P2-12 | Accuracy dashboard | P2-11 | no | - | NOT_STARTED |

### Phase 3 — Managed-Service Marketplace  (progress: 9/9 DONE ✅)

| ID | Task | Depends on | Gate | Owner | Status |
|----|------|-----------|------|-------|--------|
| P3-01 | Order model and state machine | P0-03, P0-06 | no | aarit | DONE |
| P3-02 | Pricing and turnaround rules | P3-01 | no | aarit | DONE |
| P3-03 | Order placement flow | P3-02, P1-01 | no | aarit | DONE |
| P3-04 | Estimator assignment and capacity | P3-01 | no | aarit | DONE |
| P3-05 | Fulfillment in the shared editor | P3-04, P1-09, P2-10 | no | aarit | DONE |
| P3-06 | QA workflow | P3-05 | YES | aarit | DONE |
| P3-07 | Delivery, acceptance, and dispute | P3-06 | no | aarit | DONE |
| P3-08 | Internal ops dashboard | P3-04 | no | aarit | DONE |
| P3-09 | Order audit trail | P3-01 | no | aarit | DONE |

### Phase 4 — Billing, Scale, and the Flywheel  (progress: 6/8 DONE)

| ID | Task | Depends on | Gate | Owner | Status |
|----|------|-----------|------|-------|--------|
| P4-01 | Subscriptions and seats | P0-06 | no | aarit | DONE |
| P4-02 | Usage metering and quotas | P2-03, P1-13 | no | aarit | DONE |
| P4-03 | Retainers and draw-down | P3-03 | no | aarit | DONE |
| P4-04 | Estimator payouts | P3-07 | YES | aarit | DONE |
| P4-05 | Training and evaluation pipeline | P2-11 | YES | - | NOT_STARTED |
| P4-06 | Model promotion and rollback | P4-05, P2-02 | YES | - | NOT_STARTED |
| P4-07 | Assemblies | P1-10 | no | aarit | DONE |
| P4-08 | Integration exports | P1-13 | no | aarit | DONE |

### Phase 5 — Hardening and Growth  (progress: 5/6 DONE; P5-06 engineering done, external pen-test IN_REVIEW)

| ID | Task | Depends on | Gate | Owner | Status |
|----|------|-----------|------|-------|--------|
| P5-01 | Multi-region readiness and DR drills | none | no | aarit | DONE |
| P5-02 | SSO and MFA for enterprise | P0-05 | no | aarit | DONE |
| P5-03 | Outbound webhooks | P0-09 | no | aarit | DONE |
| P5-04 | Advanced collaboration | P1-07 | no | aarit | DONE |
| P5-05 | Cloud-storage import | none | no | aarit | DONE |
| P5-06 | Security review and penetration test | P0-07 | no | aarit | IN_REVIEW |

**Totals:** 59 tasks. 11 DONE / 1 IN_PROGRESS / 1 IN_REVIEW / 0 BLOCKED / 46 NOT_STARTED. Update these counts as you go.

---

## 5. Gate checklist

A gate must have passing tests before the phase it belongs to is considered finished and the next phase begins. Mark each PASS only when its task is DONE and verified.

- [x] P0-07 — Org isolation proven fail-closed at the data layer (RLS + non-superuser role + withOrgScope). Proven on projects & memberships; the guard test forces RLS on every future org_id table as Phase 1 entities land. (Signed-URL scoping/expiry → P1-01.)
- [x] P0-10 — Seed trades & starter conditions exist, load idempotently, and are visible to new orgs (tested; unit↔type validated). **GATE PASSED (2026-07-06): the domain estimator signed off** on the trade list & condition units in `apps/web/server/modules/trades/seed-data.ts` — the catalog is now approved, not provisional.
- [~] P1-06 — Viewer engine (viewport + DZI tile math), authorized tile-serving, the pan/zoom canvas component, AND the **`/sheets/[id]` viewer page** are built & tested (12 viewer tests + 3 tile-serving). **Visual smoke PASSED**: a level reconstructed *from its DZI tiles via the viewer's own viewport+dzi math* stitches **seamlessly** (diagonals straight across all 12 tile boundaries, circle unbroken — the gate's "no gaps/misalignment" scenario, inspected as an image). Routes dev-smoked (page → sign-in redirect; sheet/tile APIs 401 without auth). **Only the human steps remain: the 60fps/<300ms perf budget on representative hardware (not measurable headlessly), and a logged-in browser render of a real processed sheet.**
- [x] P1-11 — Quantity rollups are server-authoritative and tamper-proof (server core: client submits geometry only, server computes raw_value + recomputes rollup from the full set; add/edit/delete + convergence tested). Client "recomputing" indicator + debounce are frontend (P1-09).
- [x] P1-14 — Exports match rollups exactly. **PASSED:** `parity.ts` re-parses every number out of each rendered export (SUMMARY/DETAILED/BY_TRADE) and reconciles it to the authoritative `QuantityRollup` at full precision (pre-rounding); a deliberately introduced rounding discrepancy (line-item quantity, line-item cost, grand total) is caught. Pure tests (5) + a DB+storage representative-takeoff gate test, all green.
- [x] P2-05 — Unconfirmed-scale sheets excluded from final reports. **PASSED:** final reports recompute each condition's quantity counting only CONFIRMED-scale sheets (`aggregateForConditionConfirmedScale`); measurements on UNSET/AUTO-scale sheets are provisional and excluded, and the excluded sheets are surfaced as a `PROVISIONAL` note (never silent — no "skip"). Confirming a sheet's scale promotes its quantities. DB-tested; P1-14 export↔rollup parity still holds.
- [x] P2-11 — Every AI correction captured as feedback. **PASSED:** every review action (accept/reject/edit-geometry/reclassify/add-missed/bulk-accept) writes exactly one `DetectionFeedback` row in the same transaction as the state change, with full provenance (action, before/after geometry, from/to class, originating model_run, actor + role). DB-tested incl. the reconciliation case (N actions → N rows, nothing dropped). Org training opt-out is honored at assembly time (Phase 4), not by dropping rows.
- [x] P3-06 — QA checklist gates delivery. **PASSED:** DELIVERED is reachable only from IN_QA (state machine), and `qaService.approve` is blocked unless the auto-checks pass (scale CONFIRMED on every sheet; all requested trades covered by the takeoff's conditions) AND the reviewer attests (quantities spot-checked, report renders); a failing order is returned to REVISIONS with notes. The checklist + notes are recorded on the immutable order audit log. DB-tested incl. the revisions loop preserving prior notes. (Checklist contents are the domain estimator's to own — §7 TBD.)
- [x] P4-04 — Payouts only on acceptance or auto-accept. **PASSED:** `payoutService.processAcceptedOrder` creates a `PayoutRecord` strictly when `order.status === 'ACCEPTED'` (and an estimator is assigned) — a DELIVERED/DISPUTED/unaccepted order produces NO payout (disputes never reach ACCEPTED, so they can't pay). Exactly-once via unique `order_id`; created PENDING + committed before the external provider transfer, so a failed transfer stays PENDING (owed, retriable) and a reversal (PAID→REVERSED) preserves the amount + transfer ref for audit. Wired into auto-accept (settles each after the transitions commit) + the accept route (server-side; transfers never client-initiated). DB-tested incl. the gate (delivered/disputed → no payout), exactly-once, failed-transfer reconciliation, auditable reversal, and auto-accept end-to-end. (Payout rate 0.6 + Stripe Connect rail are provisional — §7 TBD.)
- [ ] P4-05 — Training data respects opt-outs; benchmark frozen
- [ ] P4-06 — Model promotion blocks regressions; rollback works

---

## 6. Blockers log

Record anything stopping progress. Remove or mark resolved when cleared. Keep newest at the top.

| Date | Task | Blocker | Needs | Owner | Status |
|------|------|---------|-------|-------|--------|
| 2026-06-28 | P2-*, P4-05, P4-06, P5-04 | **GPU compute home undecided — THE standing top blocker for the remaining backlog.** The app/data planes (Vercel + Neon + Upstash + R2) are live and carry every non-GPU phase; but the processing/AI/realtime planes need a second compute home that Vercel can't host: the GPU inference pool (Phase-2 detection stages P2-06/07 + orchestration), the standalone workers (`worker-files`/`worker-exports`), the persistent **WebSocket gateway** (`apps/realtime` for live presence/collab push), and the cron schedulers (webhook retry-sweep, auto-accept, DR-drill). All the app-plane seams that feed these are built + tested (stage contracts, `ModelRun` ingestion, presence model, `deliverDue`, `autoAcceptExpired`). | **Owner decision (TBD since Phase 2):** pick the host — serverless GPU (Modal/Replicate) or a container host (Render/Fly/AWS). Unblocks **P4-05, P4-06**, Phase 2's inference half (5/12→12/12), the realtime gateway (P5-04 live push), and the worker deployables. | aarit | OPEN |
| 2026-06-28 | P5-06 | **External penetration test not yet commissioned.** The engineering substance is done (adversarial regression suite + `docs/security/security-review.md` + `docs/security/pentest-plan.md` with the findings workflow); the GATE needs the real external engagement + findings remediation. | Engage a security firm against the scope in `pentest-plan.md`; track findings OPEN→REMEDIATED→VERIFIED (each gets a regression test + re-test). | aarit | OPEN |
| 2026-06-28 | P4-01/03/04, P5-02/05 | **Provider seams await real external accounts/credentials.** Stubs are wired + tested behind clean interfaces; productionization is a localized swap. | Real **Stripe** (Billing + Connect payouts); the **SAML/OIDC IdP** via Auth.js (→ `provisionEnterpriseLogin`); **Drive/Dropbox/OneDrive OAuth** adapters (→ `CloudStorageProvider`); a real webhook HTTP **sender** (`httpWebhookSender` exists, needs the scheduler). | aarit | OPEN |
| 2026-06-20 | P1-13 | **MARKED_PLANS export deferred.** The three data templates (SUMMARY/DETAILED/BY_TRADE) ship as CSV; marked-plans (burning the vector overlay onto the sheet rasters at the right positions) is rejected at request time. It needs a raster-composite pipeline (reconstruct full-res from the DZI tiles + draw overlays + emit PDF/PNG) that belongs with the worker-exports image stack, not the request path. | Implement when the standalone `apps/worker-exports` / image pipeline lands (with the Phase-2 compute home). Also: per-plan-quota **usage metering** on export is deferred to Phase 4 billing — the `reports` row already IS the meterable event. | aarit | ACCEPTED |
| 2026-06-19 | P1-02 | **Standalone `apps/worker-files` deployable deferred.** The ingestion pipeline + queue consumer (`drainOne`) live in `apps/web/server/modules/ingestion` (reusing the data/storage/org-scope layers) and are fully tested; the separate long-running worker process is a thin loop around `drainOne`. | Build `apps/worker-files` (loop + lifecycle + Docker) when the Phase-2 compute home is chosen; likely extract the data+storage layers into packages then so the worker imports them cleanly instead of from `apps/web`. Also: multi-file plan sets currently order pages by (source_file_id, index_in_set) — a global cross-file ordinal is a later refinement. Pipeline marks PROCESSED after split (raster/tile/extract P1-03/04 will insert steps before PROCESSED). | aarit | ACCEPTED |
| 2026-06-19 | P1-01 | **Multipart/resumable upload deferred** to a later P1-01 increment; increment 1 ships the contracts + data model + validation core, and the planned increment 2 does single-PUT presigned uploads + verify-on-complete + enqueue. Resumability (S3 multipart: create → presigned parts → complete/abort) is the harder sub-part — consistent with the repo pattern of shipping a tested core first (cf. P1-08/P1-11). | Increment 2 (service/routes/enqueue), then multipart. Decide per-part size + client resume protocol. | aarit | ACCEPTED |
| 2026-06-19 | P1-01 | Increment-1 **integration tests run in CI, not locally** — Docker Desktop is off, so the DB/MinIO-backed tests (incl. the org-RLS guard over the new `plan_sets`/`source_files` tables) can't run on this machine. Pure unit tests (validation, contracts) pass locally. | Start Docker Desktop for local integration runs, or rely on the green CI pipeline. | aarit | ACCEPTED |
| 2026-06-14 | P1-11 | Measurement geometry stored as **JSONB** (normalized coords) for now; quantities computed via the pure `@takeoff/geometry` package, not PostGIS `ST_*` | The single conversion path is the geometry package (spec §9 / "every quantity flows through it"); drizzle's PostGIS polygon support is weak. **PostGIS geometry columns + spatial math (ST_Length/Area/intersects) are deferred** to when spatial ops are needed (snapping/overlap, P1-07/P2). PostGIS requirement (P0-04) still stands for that. | P1-11, P1-07, P2-06/07 |
| 2026-06-22 | P2-02/P2-03 (early start) | Built the **app-plane core** (ModelRun persistence + AI candidate ingestion) before the GPU host / live inference exists | Risk accepted: ModelRun lifecycle and candidate ingestion are pure app-plane state that drives off the P2-01 stage contracts and is fully testable headlessly with a synthetic `SheetInferenceResult` (as the tests do). The GPU pool + live Python pipeline + the `jobs:inference` worker are a separate, host-dependent concern. The enqueue side is wired now; the consumer lands with the compute home. | P2-02 (GPU pool + skeleton), P2-03 (live orchestration + partial-failure across real sheets), P2-09/10 (review UI over the candidates) | aarit | ACCEPTED |
| 2026-06-14 | P1-11 (early start) | Building the rollup SERVER core before P1-09 (drawing tools, frontend) | Risk accepted: rollups are computed from the authoritative measurement set; that set can be created with synthetic measurements (as the tools will) and tested headlessly. The tools UI is a separate frontend task. | aarit | ACCEPTED |
| 2026-06-14 | P1-10 | Added a **minimal `takeoffs` table** (spec §5.3) to anchor conditions; full takeoff lifecycle is a later task. Per-condition quantity math (waste/derived/extended-cost) lives in `conditions/quantities.ts` (pure, on @takeoff/geometry); the persisted rollup over a measurement set is P1-11. `plan_set_id` on takeoffs is nullable until plan sets exist. | P1-10; P1-11 (rollups), takeoff lifecycle |
| 2026-06-14 | P1-08 | Built the pure `@takeoff/geometry` package ahead of its registry deps (P1-07 viewer) and the P0-10 gate | Risk accepted: geometry/scale/quantity math is pure, UI-free, the foundation P1-09/P1-11 depend on. Package DONE & tested (25 tests incl. metric/imperial calibration, holes, self-intersection, e2e calibrate→quantity). The two-point scale-calibration UI part of P1-08 still waits for the viewer (P1-06/07). | aarit | DONE (pkg) |
| 2026-06-13 | P0-06 | Started before its dependency P0-05 (identity provider) is DONE | Risk accepted: build accounts domain + RBAC against local Postgres; the authenticated-user identity is abstracted behind an `AuthContext` resolver so P0-05's OIDC/JIT-provisioning plugs in without rework. No live auth needed to build/test the domain. | aarit | ACCEPTED |
| 2026-06-15 | (resolved) | All Phase-0 cloud integrations now provisioned | ~~GitHub~~, ~~app shell~~, ~~integration wiring~~, ~~R2~~, ~~Neon~~, ~~Upstash~~, ~~OIDC (Entra ID)~~ — all DONE & verified; `AUTH_*` + data env set in Vercel (Prod/Preview/Dev). Hosted app is deployable. Remaining cloud item is a P0-08 concern (CI deploy gating), not a blocker. | aarit | RESOLVED 2026-06-17 |
| - | - | local development is unblocked (docker-compose stack works) | - | - | - |

---

## 7. Decision log

Record decisions that future tasks depend on, especially the ones with no single right answer. This prevents re-litigating settled choices and explains why something is the way it is. Newest at the top. Mirror significant entries into a proper ADR file under docs/adr.

| Date | Decision | Context / rationale | Affects |
|------|----------|---------------------|---------|
| 2026-06-13 | Org isolation: **Postgres RLS** (FORCE) keyed on a per-tx `app.current_org_id`, set via `withOrgScope`; tenant access uses a **non-superuser role** (`takeoff_app` locally) so RLS bites; admin/identity uses the superuser conn. `enable_org_rls(table)` applied per customer-owned table; an introspection **guard test** fails the build if any org_id table lacks RLS. Storage keys namespaced `org/{id}/…`. | P0-07 GATE; P1-* (every customer-owned table must call enable_org_rls); signed-URL scoping → P1-01 |
| 2026-06-13 | Data layer: **Drizzle ORM + drizzle-kit** (Postgres). RBAC lives in **`@takeoff/auth`**; accounts domain + data layer live in **`apps/web/server`** (framework-agnostic; Next.js wiring added in P0-05) | P0-06. Drizzle chosen over Prisma for first-class PostGIS/custom-type support and Neon-serverless fit. `pg` driver locally; Neon serverless driver added for Vercel later. App code already lands in its final home (`apps/web/server`) so P0-05 only adds Next routes/auth, not a move. | P0-06, P0-07, P1-* (every entity, migrations) |
| 2026-06-19 | Extraction (P1-04): **pdfium text layer** (not OCR) for vector PDFs; **per-field USER/EXTRACTED provenance**; **plain `search_text`** column | Most construction drawings are text PDFs, so pdfium's text layer gives deterministic, fast, testable metadata without a heavy/slow OCR engine (tesseract). **OCR for raster-only sheets is deferred** behind the pluggable `MetadataExtractor` (returns nothing for now → graceful blank). Per-field `*_source` makes "user edits win over re-extraction" precise (vs a single row-level flag). Search starts as a normalized `search_text` column (the "index entry"); upgrade to Postgres **FTS (tsvector/GIN)** or trigram when search lands (CLAUDE.md: "Postgres FTS early"). | P1-04, search (later), P2 (AI re-extraction) |
| 2026-06-19 | Rasterize/tile (P1-03): **pdfium (wasm)** renders PDFs, **sharp** tiles to **DZI** (256px/overlap 1, PNG) + thumbnail; **working DPI = 150 (provisional)** | pdfium-wasm is host-agnostic (no native poppler/ghostscript binary) so it runs on Vercel-adjacent + the Phase-2 worker host alike; sharp's `.tile()` is battle-tested DZI. DZI is the OpenSeadragon-style standard the viewer (P1-06) will consume — conventions are pinned in `@takeoff/contracts/tiles` so producer/consumer can't drift. **150 DPI is a provisional default for the "working raster DPI" TBD** (STATE §7) — revisit with the estimator (too low hurts detection, too high explodes storage). PNG tiles (not JPEG) to avoid artifacts on line art. Caveat to verify at P1-06: pdfium's 4-channel bitmap is fed to sharp as-is; if real PDFs show swapped colors, add a BGRA→RGBA swap. | P1-03, P1-06 (viewer), P2 (detection DPI) |
| 2026-06-20 | Undo/redo (P1-12): undo-delete / redo-create **restore the soft-deleted row** rather than re-inserting | Re-inserting would mint a new id, breaking redo references and any links to the measurement; restoring (`deleted_at = null`) keeps the id stable across the whole history and lets the server recompute the rollup to the exact prior value. Editing applies **optimistically with a snapshot-revert** so a failed sync never leaves phantom geometry (the task caveat). | P1-12, `measurementsService.restore`, P3-05 (shared editor reuses history) |
| 2026-06-19 | Overlay (P1-07): vectors on a **separate canvas sharing the tile viewer's `Viewport`**; hit-test against **geometry**, not pixels | One shared transform guarantees pixel-alignment at every zoom (the spec §9 caveat — geometry is only ever normalized sheet coords; screen positions are derived each frame, never stored). A second canvas keeps thousands of objects cheap to repaint and lets the tile layer stay `pointer-events:none` while one pointer surface (the wrapper) distinguishes pan-drag from select-click. Hit-test uses a zoom-scaled tolerance (`pickPx / scale`) so the pick target is a constant screen size. | P1-07, P1-09 (draw tools add measurements), P2-09 (AI candidate layer reuses it) |
| 2026-06-19 | Viewer (P1-06): hand-rolled **Canvas 2D deep-zoom** engine (not OpenSeadragon); tiles served via an **authorized proxy route**, not public/signed-per-tile | A small, owned engine keeps the viewport transform shareable with the overlay (P1-07) and the math unit-testable (vs a black-box lib). Canvas 2D (not WebGL) is enough for tile blitting + simple pan/zoom and far simpler; revisit WebGL only if the perf gate fails on real hardware. Tiles are private (org-scoped storage), so a proxy route rebuilds the key from org+sheet (RLS) and streams — avoids per-tile signed URLs and keeps isolation. Conventions pinned in `@takeoff/contracts/tiles` so producer (P1-03) and consumer can't drift. | P1-06, P1-07 (shared viewport), CDN (cache the tile route later) |
| 2026-06-19 | Uploads data model (P1-01): **`org_id` denormalized** onto `plan_sets` and `source_files`; `byte_size` is **bigint**; type allow-list cross-checks **extension + MIME**; completion verifies **size always, checksum when storage computed it** | Spec §5.2 reaches SourceFile via plan_set → org, but our RLS keys on a per-row `org_id` column (P0-07), so every customer-owned table carries `org_id` for uniform fail-closed isolation. bigint avoids overflow on large CAD sets. Never trust the client's content-type alone (spec §10.2): a `.exe` labelled `application/pdf` is rejected at the boundary; magic-byte sniffing is deferred to ingestion (P1-02). Verify-on-complete HEADs the object for real size (+ S3 SHA-256 where available). Launch ceilings: 500 MiB/file, 200 files/set, 2 GiB/set (provisional). | P1-01, P1-02 (ingestion), P1-03 (sheets) |
| 2026-06-19 | Observability (P0-09): **structured JSON logs + a correlation id**, in a shared edge-safe **`@takeoff/observability`** package; contract (`CORRELATION_ID_HEADER`/`Traceable`) in `@takeoff/contracts` | The correlation-id contract must exist before Phase 1 spreads across services (the P0-09 caveat). Package is edge-safe (no `node:async_hooks`) so middleware AND future workers share it; the Node-only `AsyncLocalStorage` request context lives in `apps/web/server/platform`, not the package. Metrics are **log-based events** (`event:"metric"`/`"error"`) because serverless has no shared counters — Vercel/log-drain aggregates them. Tracing is a **scaffold** (`instrumentation.ts`) gated on `OTEL_EXPORTER_OTLP_ENDPOINT`; full OTEL wiring deferred until the Phase-2 second compute home exists. Dashboard + error-rate alert are manual Vercel config. | P0-09, P1+ (all services log under the contract), P2 (tracing) |
| 2026-06-19 | CI/CD (P0-08): **GitHub Actions** for lint/format/typecheck/test/build + **Vercel Git integration** for deploy | Matches the Vercel hosting model (CLAUDE.md §6): Actions runs `.github/workflows/ci.yml` on push/PR to main against ephemeral service containers (Postgres+PostGIS, Redis) + a MinIO container, reusing `db:bootstrap` to mirror the local stack. Deploy is Vercel's git integration (push to `main` → production; PRs → preview). Migrations are a **separate manual workflow** (`db-migrate.yml`, `workflow_dispatch`, `production` environment) using a `DATABASE_URL` repo secret — never in the build path. **Prod gating = GitHub branch protection** requiring the CI check on `main` (recommended; not auto-enabled to avoid breaking solo direct-push — `gh` not installed). | P0-08, P0-09 (obs), every future PR |
| 2026-06-17 | OIDC identity provider: **Microsoft Entra ID** (single-tenant app registration "Takeoff Platform") | User is on Azure; Entra is a standards-compliant OIDC provider that works with our generic Auth.js `oidc` provider (issuer = `https://login.microsoftonline.com/<tenant>/v2.0`, discovery issuer matches exactly). Set up via **Azure CLI** (`az ad app create` + `credential reset`), not browser. Single-tenant (`AzureADMyOrg`) = only the Default Directory's users sign in — fine for setup/managed-service; switch to `AzureADMultipleOrgs` if external contractors with their own MS orgs must log in. Added `email`+`preferred_username` optional id-token claims because Entra omits `email` by default and our JIT provisioning needs it. Tenant is a **personal-account Default Directory** (`864774d8…`) — revisit if a work tenant becomes preferred. | P0-05, P5-02 (SSO/MFA), all authed routes |
| 2026-06-17 | Hosted data layer provisioned via **Vercel Marketplace**: **Neon Postgres** (Postgres 17 + PostGIS 3.5) and **Upstash for Redis**, both connected to the `takeoffs` project | `vercel integration add neon` / `upstash/upstash-kv` auto-creates the resources, injects env across all envs, and pulls to `.env.local`. Neon owner (`neondb_owner`) has `rolbypassrls=true`, so a separate **`takeoff_app`** (NOSUPERUSER NOBYPASSRLS) role is the tenant connection (`APP_DATABASE_URL`); `db:bootstrap` automates PostGIS + role + grants idempotently (note: re-runs must not re-assert NOSUPERUSER/NOBYPASSRLS — Neon rejects attribute changes by non-superusers). Org-isolation proven on live Neon via `db:verify-role`. | P0-04 (hosted), P0-05, P1-* (all hosted data) |
| 2026-06-15 | Object storage (prod): **Cloudflare R2** | User is on Azure, but Azure Blob is NOT S3-compatible; R2 is genuinely S3-compatible so the existing `S3Storage` adapter works unchanged (endpoint = `https://<acct>.r2.cloudflarestorage.com`, region `auto`, path-style). MinIO stays the local stand-in. No code change — config only; see `docs/runbooks/integrations-setup.md` §4. | P1-01 (uploads), storage env |
| 2026-06-15 | Integrations wired (P0-05 + infra): **Auth.js v5 generic OIDC** (JIT user provisioning, JWT session, edge middleware gating the `(app)` routes; disabled when `AUTH_ISSUER_URL` unset), **ioredis** Redis client, and an **S3-compatible storage adapter** (presigned upload/download, tested vs local MinIO). **Storage choice changed: Vercel Blob → S3-compatible** (R2/S3/MinIO) — matches the spec's presigned-upload model + local parity; supersedes the 2026-06-13 Blob decision. Account setup pending — see `docs/runbooks/integrations-setup.md`. | P0-05, P1-01 (uploads), all data/auth |
| 2026-06-15 | Design system: **`@takeoff/ui`** (source-consumed React) — tokens (TS + mirrored CSS vars), `cn`, primitives (Button/Card/Badge/Stack), one shipped `styles.css` (imported once at the app root). Component tests run under **jsdom** via Vitest (`@testing-library/react`). | Foundational (not a numbered task); supports all Phase 1 frontend. Viewer primitives join here at P1-06/07. |
| 2026-06-15 | App shell: **Next.js 15 (App Router) + React 19** in `apps/web`; workspace packages via `transpilePackages`; `pg` kept external; ESLint-at-build off (repo ESLint covers it); `next-env.d.ts` gitignored (generated) | P0-05 app-shell. `apps/web` flips from a source-only TS package to a Next app; `build` is now `next build`, `typecheck` stays `tsc --noEmit`. Server domain (`server/`) unchanged, imported by future route handlers. | P0-05 (auth), all Phase 1 frontend |
| 2026-06-13 | Local dev stack: **docker-compose** with `postgis/postgis:16-3.4`, `redis:7`, **MinIO** (S3-compatible) standing in for Vercel Blob | P0-04 (local portion). Mirrors the hosted data layer so app/workers run end-to-end without cloud accounts; storage adapter makes MinIO↔Blob a config swap. Run via `pnpm dev:up`/`dev:down`/`dev:reset`. PostGIS verified (spatial column create/insert/drop; reset+up repeatable). | P0-05+, P1-* (data layer, geometry) |
| 2026-06-13 | Contract validation: **Zod**; internal packages are **source-consumed** (exports → `src/index.ts`, no emit); tests via **Vitest** | P0-02. Zod gives runtime validation + inferred static types from one definition. Source consumption (Next.js transpiles; workers built with tsup/tsx) avoids the ESM-extension footgun — `build`/`typecheck` = `tsc --noEmit`. Source packages add a local `turbo.json` (`"extends": ["//"]`, empty `outputs`) to silence Turbo's no-output warnings. | P0-03+ (all contract shapes); every TS package |
| 2026-06-13 | Workspace tooling: **pnpm 9 workspaces + Turborepo 2**; ESLint 9 flat config + Prettier 3; internal scope `@takeoff/*` | P0-01. Canonical Vercel monorepo. Cross-package deep imports blocked by ESLint `no-restricted-imports` now. **Gotcha:** `corepack enable` fails on this Windows box (EPERM writing to `C:\Program Files\nodejs`) — pnpm was installed via `npm i -g pnpm@9.15.4` into the user prefix instead. Use that, not corepack, here. | P0-02+ (all TS packages/apps) |
| 2026-06-13 | Hosting: app plane on **Vercel**, source of truth on **GitHub** | Product is hosted on Vercel; GitHub drives deploys (Vercel git integration) + GitHub Actions for CI. Vercel cannot run GPU/long workers/persistent WebSockets, so hosting is split by plane. | P0-01, P0-04, P0-08, all app-plane tasks |
| 2026-06-13 | Frontend + app API: **Next.js (App Router)** full-stack on Vercel | Vercel-native; collapses the spec's Vite SPA + standalone NestJS into one app. Domain logic kept framework-agnostic under `apps/web/server` so it stays portable. Drop `sdk-client`. | P0-01, P0-02, P1-*, all API modules |
| 2026-06-13 | Data stores: **Neon Postgres + PostGIS**, **Upstash Redis**, **Vercel Blob** | Vercel Marketplace integrations; Neon supports the required spatial extension. Least ops, tightest Vercel fit. | P0-04, P1-01, P1-08, geometry/measurements |
| 2026-06-13 | Processing/AI/realtime compute home: **TBD at Phase 2** | Phases 0–1 run on Vercel + Neon + Upstash + Blob. GPU inference, long workers, and the WebSocket gateway need a second host (serverless GPU like Modal/Replicate, or a container host like Render/Fly/AWS) — decide when AI lands. | P2-02, P2-*, P1-02..P1-04 (workers), realtime |
| - | Working raster DPI for sheets: **provisional 150** (in `@takeoff/contracts/tiles` `WORKING_DPI`), pending estimator sign-off | Too low harms AI accuracy; too high inflates storage and processing. 150 chosen as a balanced default to unblock P1-03; not yet estimator-approved. | P1-03, P2-04, P2-06, P2-07 |
| 2026-06-23 | Scale gate (P2-05): **the final report applies the gate, not the cached rollup** | The per-condition `QuantityRollup` stays the working/provisional total (all counted measurements, what the viewer shows); the gate lives in `buildReportData`, which recomputes scale-gated quantities for the deliverable. Keeps the viewer's live number simple, makes "provisional vs final" an explicit report-time distinction, and leaves the P1-14 parity surface (render vs report data) intact. Sheet-less measurements are treated as ungated (no sheet scale to confirm). | P2-05, P1-13/14 (reports), P4 (usage metering reads reports) |
| 2026-06-22 | Cross-plane stage contracts (P2-01): **Zod is canonical → emit one JSON Schema artifact → Python validates against it** (not hand-copied Pydantic) | The Plan calls for the Python `contracts` to "mirror" `packages/contracts`. A hand-written Pydantic copy drifts silently. Instead Zod stays the single source of truth, `gen:schemas` emits `stage-contracts.schema.json`, and Python validates with `jsonschema` against that exact file — so the two planes enforce the same contract by construction. A TS drift test keeps the committed artifact in sync with Zod; shared fixtures prove identical validation on both planes. Pydantic models can wrap this later for ergonomics when stages are implemented (P2-04+). | P2-01, P2-03 (orchestration validates stage I/O), P2-04+ (stage impls), CI `python-contracts` job |
| 2026-06-20 | Exports (P1-13): render **straight from `QuantityRollup` at full precision**, CSV for data templates; reuse the existing billing `Report*` enums | Pulling numbers verbatim from the cached authoritative rollup (never recomputing) makes an export equal the rollup by construction — the cleanest way to satisfy the P1-13 caveat and pre-stage the P1-14 parity gate. Exports carry full precision; display rounding stays the viewer's concern so the parity check compares pre-rounding values. CSV avoids a heavy spreadsheet dep (opens in any spreadsheet); status uses the pre-existing `QUEUED` (not a new `PENDING`). | P1-13, P1-14 (parity gate), P4-08 (integration exports) |
| 2026-06-20 | Self-intersecting polygon policy: **REJECT** | Auto-correcting an ambiguous outline could silently produce a wrong area; better to make the user fix it. Enforced in `finishDrawing` via `@takeoff/geometry`'s `isSimplePolygon`. | P1-09, geometry package |
| 2026-06-13 → **RESOLVED 2026-07-06** | Launch trade list & condition units: **APPROVED** (6 trades / 14 conditions, `seed-data.ts`), unit↔type machine-validated. The domain estimator signed off on 2026-07-06; the catalog is no longer provisional and the **P0-10 GATE is PASSED**. | Wrong units corrupt every quantity downstream; the catalog is now estimator-approved. | P0-10 (done), P1-10 |
| 2026-06-27 | Managed-service **dispute / auto-accept window: provisional `72h`** (P3-07, `DISPUTE_WINDOW_HOURS`) — after delivery the customer has this long to accept/dispute, else the order auto-accepts. The MECHANISM (configurable window, `disputeDeadline` exposed on the order, the auto-accept sweep) is built; the **number is the owner's to set**. | The window must be explicit + visible to the customer (the caveat); 72h is an engineering default. Auto-accept is gated to delivered-past-window; no payout until ACCEPTED/auto-accept (Phase 4). | P3-07, P4-04 (payout on acceptance) |
| 2026-06-27 | Managed-service **QA checklist contents: a sensible default set in place** (P3-06) — two auto-blocking checks (scale confirmed per sheet; requested trades covered) + two reviewer attestations (quantities spot-checked; report renders). The MECHANISM (gate + recording on the audit log) is built; the exact checklist items/thresholds are the **domain estimator's to own**. | Engineering provides the gate; the domain estimator defines the checklist (the caveat). Items live in `qa.ts`; making them data/config is a later refinement. | P3-06, P3-07 (delivery follows QA) |
| 2026-06-26 | Managed-service **pricing/turnaround numbers: provisional defaults in place** (`pricing/seed.ts`, 6 rows by tier×priority) — the MECHANISM is built (data-driven rules table; business edits rows, no deploy) but the actual **numbers are a placeholder the owner/founder must set** | Engineering provides the mechanism; the owner sets the prices (P3-02 caveat). The seed is non-destructive so business-tuned values survive a redeploy. Tests assert relative properties (RUSH > STANDARD, etc.) + the no-zero/negative invariant, not the magic numbers. | P3-02, P3-03 (placement shows the quote), P4 (billing charges it) |
| 2026-06-27 | **Plan catalog entitlement numbers: provisional** (P4-01, `billing/catalog.ts` `PLAN_CATALOG`) — per-tier seat limits + quota entitlements (AI runs/mo, exports/mo, managed-orders flag) for FREE/STARTER/PRO/RETAINER. The MECHANISM (webhook → entitlements → org plan/seats/status; seat enforcement) is built + tested; the actual **numbers + the tier↔Stripe-price mapping are the owner's to set**. Catalog lives in code (not a DB table) because authz depends on it — tuning is a deploy. Also TBD: the precise org **restricted-state policy** per subscription status (current mapping: PAST_DUE→PAST_DUE, PAUSED→SUSPENDED, CANCELED/INCOMPLETE→FREE/ACTIVE). | Engineering provides the mechanism; the owner sets entitlements (the P4-01 caveat parallels pricing). Tests assert relative properties (higher tier ⇒ more seats) + the status→state mapping, not the magic numbers. | P4-01, P4-02 (quotas read these), P3-03/P4-03 (Stripe price↔tier) |
| 2026-06-28 | **Estimator payout rate: provisional `0.6`** (P4-04, `service-ops/payouts.ts` `ESTIMATOR_PAYOUT_RATE`) — the estimator's share of a managed order's price. The MECHANISM (gated payout on acceptance, PENDING→PAID via the provider seam, reversal, reconciliation) is built + tested; the **actual rate/split is the owner's** (could become a per-estimator or per-tier rate card later). Also TBD: the **Stripe Connect** payout rail wiring behind `PayoutProvider`. | Engineering provides the mechanism; the owner sets the split (the P4-04 caveat parallels pricing/entitlements). Tests assert the formula + gate, not the magic number. | P4-04, Stripe Connect (payout rail) |
| 2026-06-27 | **Quota over-limit policy: provisional** (P4-02, `billing/quota.ts` `QUOTA_POLICY`) — what crossing each metric's monthly quota does: **AI runs = BLOCK** (FREE excludes AI entirely, limit 0), **exports = OVERAGE** (allowed, the over-quota record is flagged `billed`), **managed orders = OVERAGE/unlimited** (billed per-order at placement, never count-capped). The MECHANISM (`block | warn | overage-charge` per the card) is built + tested; the **chosen policy per metric + whether overage actually charges is the owner's** (and depends on Stripe metered billing landing). | The card says "behaves per policy (block, warn, or overage-charge as configured)" — engineering provides all three outcomes; the owner configures which. Overage records (`usage_records.billed=true`) are the charge signal for Stripe metered billing later. | P4-02, P4-01 (entitlement limits), Stripe metered billing |
| - | Auto-accept confidence thresholds per class: start conservative | Trust depends on not auto-accepting wrong candidates | P2-10, P4-06 |
| - | Dispute and auto-accept windows for orders: TBD | Must be explicit and customer-visible | P3-07, P4-04 |

> Replace each TBD with the chosen value and the date once decided. Do not start a dependent task on a TBD without logging the assumption you are proceeding under.

---

## 8. Environment and setup state

Track whether the shared scaffolding actually works, separate from feature progress. A coder resuming cold needs to know what is already standing.

- [x] Repository cloned and builds from clean checkout (P0-01) — `pnpm install && pnpm build` green; lint/format gates verified
- [x] Git remote configured — `origin` = https://github.com/aaritch/takeoffs.git; `main` tracks `origin/main` (all commits pushed). Future commits push with plain `git push`.
- [x] Contracts package importable across workspaces (P0-02) — `@takeoff/contracts` (Zod); cross-workspace type resolution verified
- [~] Dev environment (P0-04): **local** docker-compose stack up & PostGIS spatial column verified; **hosted** now LIVE — **Neon Postgres** (PostGIS, `takeoff_app` RLS role, migrated+seeded, org-isolation proven on the live DB), **Upstash Redis** (round-trip OK), **Cloudflare R2** (round-trip OK). All env vars (`DATABASE_URL`, `APP_DATABASE_URL`, `REDIS_URL`, `S3_*`) set in Vercel across Prod/Preview/Dev and pulled to `.env.local`. **Note:** `.env.local` (vercel pull) points local `pnpm dev` at HOSTED services and overrides `.env` — rename/remove it to develop against the local docker stack. Only OIDC provisioning still pending.
- [x] Next.js app shell + **Auth.js OIDC LIVE** (P0-05): JIT provisioning, JWT session, middleware route-gating, sign-in/out UI — wired to **Microsoft Entra ID** (single-tenant app reg). Verified: sign-in 302s to Entra authorize with correct client_id/redirect_uri/PKCE/scope; `/api/health` auth:true. Redis + S3 also live. (Final human confirmation: complete one browser login.)
- [~] Hosted deploy: **app is LIVE on Vercel production** (`takeoffs-aaritchs-projects.vercel.app`), deployed manually via `vercel deploy --prod`. Project settings: rootDirectory=`apps/web`, framework=`nextjs`, sourceFilesOutsideRootDirectory=true, ssoProtection=preview-only. **Git auto-deploy NOT connected yet** (P0-08).
- [x] CI runs lint, tests, build, and deploys (P0-08): **GitHub Actions `ci.yml` green** (lint/format/typecheck/test/build vs ephemeral Postgres+PostGIS/Redis/MinIO). **Git-connected to Vercel** (push `main` → production READY, PRs → preview). **`db-migrate.yml`** = explicit manual migration workflow (set a `DATABASE_URL` repo secret to use it). Optional: branch protection on `main` to gate prod by approval.
- [x] Correlation id traces a request across services (P0-09) — `x-correlation-id` minted/propagated by edge middleware + bound to a request-scoped logger via `withRequestContext`; the contract (`CORRELATION_ID_HEADER`/`Traceable`) lives in `@takeoff/contracts` for the broker/workers to carry in Phase 1. (Vercel error-rate alert is a manual dashboard config — see `docs/runbooks/observability.md`.)
- [ ] GPU worker pool provisioned and scales to zero (P2-02)
- [ ] Payment provider connected in test mode (P4-01)

---

## 9. Cross-cutting rules (never violate, on any task)

- Quantities are computed server-side from authoritative geometry; never trusted from the client or from raw AI output.
- Any work longer than roughly one second runs as an idempotent, retriable background job.
- A sheet's quantities count toward a final report only after its scale is confirmed.
- Org isolation is enforced at the data layer and fails closed; every new customer-owned table gets the org filter.
- Every cross-boundary shape comes from the shared contracts package; no local re-declarations.
- Every model change is measured against the frozen benchmark and never regresses it.
- Money is stored in integer minor units; timestamps in UTC.

---

## 10. Session handoff template

Copy this block into Section 1's notes (or a running log) at the end of each session so the next person resumes instantly.

- Date / author:
- Tasks moved to DONE this session:
- Task left IN_PROGRESS and exactly where it stands:
- New blockers raised (and which tasks they stop):
- New decisions made (also added to Section 7):
- Recommended next task for the following session:
- Anything the next person should not waste time rediscovering:

---

## 11. Definition of done reminders (per phase)

- **Phase 1 done:** upload, process, measure by hand against a confirmed scale, organize into conditions, export a report whose numbers match the on-screen rollups exactly.
- **Phase 2 done:** uploads produce reviewable AI candidates; accept/reject/edit works; unconfirmed-scale quantities excluded from final reports; every correction captured; accuracy metrics visible.
- **Phase 3 done:** order, assign, fulfill, QA, deliver, accept or dispute, all transitions audited and SLAs tracked.
- **Phase 4 done:** charge subscriptions and usage, draw retainers, pay estimators on acceptance, promote a retrained model only when it clears the benchmark.
- **MVP:** Phases 1 through 3 complete and stable; Phase 4 billing live enough to take revenue.
