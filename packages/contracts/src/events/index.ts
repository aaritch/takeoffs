// Real-time event payload schemas live here (subscribed per takeoff_id / plan_set_id /
// model_run_id; see spec §12.3). The channel carries deltas only — the database remains
// authoritative. Filled from Phase 1.
export {};
